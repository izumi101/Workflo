# CLAUDE.md — Workflo Operating Manual

> This file is auto-loaded by Claude Code at the start of every session.
> It is the **single source of truth** for how work is done on Workflo.
> Claude MUST read the Progress Log before acting and MUST append to it after every meaningful step.

---

## 1. What Workflo Is

Workflo is a **Jira alternative** — the goal is to build a project/issue tracker that beats Jira on:
- **Speed** — instant, keyboard-first UI (Linear-style), no slow page loads.
- **Simplicity** — no JQL barrier, no heavy admin/permission schemes to configure.
- **Real-time** — live collaboration (presence, live board updates) built in from day one.
- **Low notification noise** — smart, filterable notifications.

We are NOT trying to replicate every Jira feature. We deliberately cut scope (see §5).

---

## 2. Tech Stack (locked)

| Layer | Choice | Notes |
|-------|--------|-------|
| Frontend | **React 18 + TypeScript + Vite** | SPA. State: TanStack Query + Zustand. UI: keyboard-first. |
| Backend | **NestJS (Node + TypeScript)** | Modular monolith. |
| ORM | **Prisma** | Type-safe DB access. |
| Database | **PostgreSQL** | Primary datastore. Full-text search via Postgres FTS (no Elastic in MVP). |
| Cache / PubSub | **Redis** | Cache, Socket.IO adapter, BullMQ queues. |
| Real-time | **Socket.IO** | Redis adapter for horizontal scale. |
| Queues | **BullMQ** | Notifications, async automations. |
| Repo | **pnpm monorepo (Turborepo)** | `apps/web`, `apps/api`, `packages/shared`. |
| Shared types | **`packages/shared`** | Zod schemas + TS types shared FE/BE for end-to-end type safety. |

Full rationale in [docs/architecture.md](docs/architecture.md) and [docs/adr/](docs/adr/).

---

## 3. Model Routing (MANDATORY)

Route by **task ambiguity + blast radius**, not a fixed "the big model always plans" rule. Goal: don't burn Fable/Opus on easy work — save them for genuinely murky steps, and escalate *up* only when a step earns it.

| Step type | Plan on | Implement on |
|-----------|---------|--------------|
| **Trivial / clear** — touches ≤2 modules AND the approach is already specified in the roadmap / an ADR / a spec (e.g. an endpoint per spec, one more component) | Sonnet 5 plans itself | **Sonnet 5** |
| **Ambiguous / cross-cutting** — touches >2 modules, OR the approach isn't written down yet, OR it adds a new cross-cutting concern | **Opus 4.8** (main session or a planning sub-agent) | **Sonnet 5** |
| **Genuinely hard planning** — Opus honestly tried and is circling (can't reconcile the constraints) | **Fable 5** — one pass, not iterative | **Sonnet 5** |

Code + tests are always written by **Sonnet 5** (dispatch via the `Agent` tool with `model: "sonnet"`), never directly on Opus.

**Foundation / architecture review:** default to a **fresh Opus adversarial pass** ("where does this break at scale? what's the weakest assumption?") — cheap, catches ~90%. Escalate a review to **Fable** only when the foundation is genuinely non-standard AND everything else will sit on top of it — one pass, before committing.

**Verification gate (never skipped):** whoever wrote the code, the orchestrator (Opus) verifies with a real run (build / typecheck / tests) before commit. Autonomy scales the *planning depth*, not the right to skip verification — Sonnet-alone has produced false "done" reports and missed a real bug here, so a tight self-contained brief + an Opus check stays mandatory.

Rule of thumb: **spend the expensive model only where the thinking is expensive.**

---

## 4. Working Agreement (how Claude must behave)

1. **Read the Progress Log (§8) first.** Never re-decide something already decided; never redo done work.
2. **Log every meaningful step** in §8 with date + what changed + why. This is the "don't forget / don't mess up" contract.
3. **Route models by task ambiguity / blast radius, not a fixed default** (see §3) — escalate to an Opus/Fable planner only when a step earns it; never skip the Opus verification gate before commit.
4. **Small, verifiable steps.** One coherent change at a time; verify before moving on.
5. **Keep shared contracts in `packages/shared`.** FE and BE must never drift on types — change the Zod schema, both sides follow.
6. **Do not expand MVP scope** (§5) without the user explicitly approving. Park ideas in §7 Backlog.
7. **Ask before irreversible/outward-facing actions** (deploys, deleting data, public releases).
8. **Conventional Commits** for git messages. Branch off `main`; never commit straight to `main` for feature work once code exists.

---

## 5. MVP Scope (locked)

**In:**
- Auth: email/password + Google OAuth. Single workspace role model (owner/member).
- Workspace → Project → Issue data model. Issue types: Task, Bug (+ Epic as a lightweight grouping).
- Issue fields: title, description, status, assignee, reporter, priority, labels, due date.
- Fixed workflow: `To Do → In Progress → Done`.
- Kanban board with drag & drop (status change + rank ordering).
- Backlog / list view with filters.
- Issue detail page: comments + `@mentions`.
- **Real-time** updates over WebSocket (board, issue, comments, presence).
- Fast simple search (title/description/assignee) — **no JQL**.

**Deliberately OUT of MVP** (see roadmap phases 4–5):
Sprints & full Scrum mechanics · Epic-portfolio/Roadmap timeline · Automation rules · Custom workflows · GitHub/Slack integrations · Time tracking/worklogs · Service desk/SLA · SSO/audit logs/granular permission schemes · Attachments (may pull earlier if cheap).

---

## 6. Post-MVP Roadmap

1. **Private beta / dogfooding** — track Workflo's own dev inside Workflo.
2. **Public beta** (~10–50 users) — feedback + retention/time-to-action metrics.
3. **V1 release** — stabilize API, fix beta findings, add billing if monetizing.
4. **Differentiation features** — speed, keyboard-first UX, smart search (vs JQL), git integration, real-time collab.
5. **Scale / enterprise** — SSO, granular permissions, audit logs, external integrations.

---

## 7. Backlog / Parking Lot

_(Ideas that are out of current scope — do not build without approval.)_
- Sprints / Scrum boards
- Timeline / roadmap view
- Automation rules engine
- Attachments & rich media in comments

---

## 8. Progress Log (APPEND-ONLY — newest at bottom)

> Format: `YYYY-MM-DD — [PHASE] what changed — why / decision`

- **2026-07-01 — [SETUP]** Created GitHub repo `izumi101/Workflo` (public), initialized git, pushed initial commit to `main`.
- **2026-07-01 — [PLANNING]** Locked stack (React + NestJS + Postgres + Prisma + Redis), MVP scope, and post-MVP roadmap with user. Saved to memory.
- **2026-07-01 — [PLANNING]** Established model routing rule: Opus 4.8/Fable for planning, Sonnet 5 for code & tests.
- **2026-07-01 — [ARCHITECTURE]** Wrote full architecture: `CLAUDE.md`, `docs/architecture.md`, ADR-0001..0006, data model sketch, monorepo layout. Used `architecture-designer` skill. No implementation code yet (deferred to Sonnet).
- **2026-07-01 — [PLANNING]** Model routing clarified by user: **Fable 5 is the default planner, Opus 4.8 is fallback** when Fable is unavailable. Updated §3 + memory. Decided NOT to run `ln-100-documents-pipeline` now (overlaps/would overwrite these docs; revisit at beta with a tracker).
- **2026-07-01 — [ARCHITECTURE]** Presented container architecture diagram to user for review before scaffolding. Awaiting go-ahead to scaffold.
- **2026-07-01 — [SCAFFOLD]** Scaffolded pnpm+turbo monorepo: packages/shared (zod schemas), apps/api (NestJS + Prisma schema + /api/v1/health), apps/web (Vite+React+TanStack Query, health round-trip). Verified: pnpm install, shared build/typecheck, `prisma validate`, api build, web build, root `pnpm build`/`pnpm typecheck` via turbo, and a live boot of the API (`PORT=3099`) returning 200 from `GET /api/v1/health` with no DB/Redis running.
- **2026-07-01 — [AUTH]** Implemented the Auth module per ADR-0005: email/password (argon2id) + Google OAuth (Passport), JWT access token (in JSON body) + opaque rotating refresh token (sha256-hashed at rest, httpOnly+SameSite=strict cookie scoped to `/api/v1/auth`, `Secure` in prod) with **reuse detection** (a reused/revoked/expired refresh token revokes the whole `family` and 401s). Routes under `/api/v1/auth`: register(409 on dup)/login/refresh/logout/me(JwtAuthGuard)/google/google/callback. Added: root `docker-compose.yml` (postgres:16 + redis:7, healthchecks, named volume), `.env.example` split into `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` + `WEB_ORIGIN` (+ `apps/api/.env.example`), fail-fast env validation for the new secrets, `RefreshToken` Prisma model + **applied** initial migration `20260701180835_init` (all models), shared auth zod schemas (`registerSchema`/`loginSchema`/`authUserSchema`/`authResponseSchema` — never expose passwordHash), `@nestjs/throttler` global guard (5/min on login+register), `cookie-parser`, CORS locked to `WEB_ORIGIN`. **Verified (all PASSED):** `pnpm install`, shared build, `nest build`, root `pnpm typecheck` (3 pkgs), 11 unit tests (AuthService, incl. reuse-detection), and 10 supertest **e2e against a real Postgres** (register→login→me(bearer)→refresh-rotation→reuse-detection→logout), plus a live HTTP curl round-trip and a fail-fast boot check. New deps: argon2 ^0.44, @nestjs/jwt ^10.2, @nestjs/passport ^10.0.3, passport ^0.7, passport-jwt ^4, passport-google-oauth20 ^2, @nestjs/throttler ^6.5, cookie-parser ^1.4 (+types, jest/ts-jest/supertest/@nestjs/testing). Docker note: local host ports 5432/6379 were occupied by an unrelated `eventhub_*` stack on this dev machine, so the **committed** `docker-compose.yml` and both `.env.example` files map Postgres to host port **5434** and Redis to host port **6380** (container-internal ports unchanged at 5432/6379) — the migration and e2e run were against these real, docker-composed services. On a clean machine with 5432/6379 free, remap back if desired. Follow-ups before prod: real Google OAuth creds, HTTPS so `Secure` cookies apply, strong rotated JWT secrets, and consider a refresh-token cleanup job + optional access-token denylist.

- **2026-07-01 — [AUTH]** Review found a validation defect: `register`/`login` used `@UsePipes(ZodValidationPipe)` with no `@Body()` param, so request-body validation was a silent no-op (malformed input reached the service; invalid register returned 500, not 400). Fix (Sonnet): rebound to `@Body(new ZodValidationPipe(schema))` and hardened the pipe to only run when `metadata.type === 'body'`; added 4 e2e regression cases (bad email / short password / missing name / malformed login → 400). **Re-verified by orchestrator against live docker Postgres (5434): `nest build`, `pnpm typecheck`, 11/11 unit, 14/14 e2e — all PASSED.**

- **2026-07-02 — [PROCESS]** Revised model routing (§3) from "the big model always plans" to **tiered escalation by ambiguity/blast-radius**: Sonnet plans+implements clear ≤2-module steps; Opus plans cross-cutting/undocumented steps; Fable only when Opus is genuinely circling. Foundation review now defaults to a fresh Opus adversarial pass (not a paid Fable pass). Opus verification gate before commit stays mandatory. Rationale (user): stop paying premium models on trivial work. Updated §3, §4, and memory.

- **2026-07-02 — [WORKSPACES/PROJECTS]** Implemented Workspaces + Projects CRUD with the project's first authZ layer, on top of the existing JWT auth. New `apps/api/src/authz/` module: `WorkspaceMemberGuard` (resolves the target workspace via a `@ResolveWorkspaceFrom(...)` strategy — `param:id`/`param:workspaceId`/`body:workspaceId`/`query:workspaceId`/`project:id` (loads the project then uses its workspaceId) — 404s if the workspace/project doesn't exist, 403s if the caller has no `WorkspaceMember` row, else attaches `{ workspaceId, role }` to the request), `RolesGuard` + `@Roles(...)` decorator (enforces role against the attached context), `@WorkspaceContext()` param decorator. Guards run after `JwtAuthGuard`, mirroring the existing auth-module style (Zod validation via `@Body(new ZodValidationPipe(schema))`, never `@UsePipes` without `@Body()`). `apps/api/src/workspaces/`: `POST/GET /workspaces`, `GET/PATCH/DELETE /workspaces/:id`, `GET/POST /workspaces/:id/members`, `PATCH/DELETE /workspaces/:id/members/:userId` — create wraps workspace+OWNER-membership creation in one `$transaction`, slug is auto-derived from name with a uniqueness-suffix loop, add-member 404s on unknown email / 409s on existing member, and both demote-last-owner and remove-last-owner are blocked with 400 (owner count checked via `workspaceMember.count`). `apps/api/src/projects/`: `POST/GET /projects` (create/list, `?workspaceId=`), `GET/PATCH/DELETE /projects/:id` — key uniqueness is per-workspace (`@@unique([workspaceId,key])`, 409 on conflict), delete is OWNER-only, create/read/update are any member. Shared contracts: new `packages/shared/src/workspace.ts` (`workspaceSchema`, `createWorkspaceSchema`, `updateWorkspaceSchema`, `workspaceMemberSchema`, `addMemberSchema`, `updateMemberRoleSchema`, reusing `roleSchema`/`authUserSchema`), and `packages/shared/src/issue.ts` gained `updateProjectSchema` plus a stricter `createProjectSchema.key` regex (uppercase, starts with a letter). No Prisma schema/migration changes were needed — existing `User`/`Workspace`/`WorkspaceMember`/`Project` models already covered everything. **Verified (all PASSED):** `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3 pkgs via turbo), `pnpm --filter @workflo/api test` — 26/26 unit tests (11 auth + 9 WorkspacesService incl. create-makes-owner/slug-uniqueness/add-member 404+409/last-owner protection + 6 ProjectsService incl. key-conflict), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — 34/34 e2e (10 auth + 24 new: two-user flow proving non-member 403, workspace/project 404, add-member 404/409, member can read+patch-project but is 403'd on owner-only workspace-patch/project-delete/add-member, owner actions succeed, project key 409, last-owner demote/remove blocked with 400, ownership transferable then original owner safely demotable). Ran the e2e suite twice to confirm stability; docker-compose DB/Redis left running throughout, untouched. No new dependencies added, so `pnpm install` was not run. Deviation from brief: none functionally — the `@ResolveWorkspaceFrom` strategy decorator was an implementation detail (not explicitly specified) needed to make one `WorkspaceMemberGuard` work across all five resolution shapes cleanly.

- **2026-07-02 — [ISSUES]** Implemented Issues CRUD + minimal Labels on top of the Workspaces/Projects authZ layer. `apps/api/src/issues/`: `POST/GET /projects/:id/issues`, `GET/PATCH/DELETE /issues/:key`. `apps/api/src/labels/`: `POST/GET /projects/:id/labels`, `DELETE /labels/:id` (any workspace member may manage labels — no owner restriction). **Human key allocation**: `number` is allocated by an atomic `prisma.project.update({ data: { counter: { increment: 1 } } })` inside the same `$transaction` as the issue insert, using the returned counter value — verified sequential (`-1` then `-2`) both in a mocked unit test and against real Postgres in e2e. **Rank**: kept deliberately simple per plan — `apps/api/src/common/rank.ts` appends a new issue after the current last-in-status-column issue via a plain lexicographic string (`prev + "m"`, no new npm dependency); PATCH can accept a client-supplied `rank` string verbatim. Full LexoRank midpoint insertion for drag & drop is deferred (see Next up). **AuthZ**: extended `@ResolveWorkspaceFrom` with two new strategies on the existing `WorkspaceMemberGuard` — `issue:key` (parses `WF-123`, joins through all same-keyed projects to the issue, since `Project.key` is only unique *within* a workspace, not globally) and `label:id` (label → its project → workspaceId); both 404 on not-found, 403 on non-member. **Query validation gap found+fixed**: `ZodValidationPipe` was body-only by design (existing safety net), so list-query filters were silently unvalidated — added a sibling `ZodQueryValidationPipe` in the same file for `@Query(...)`. **Shared contract fix**: `createIssueSchema` originally required `projectId` in the body (leftover from the pre-Issues scaffold), which is wrong since the route supplies it — removed it from `createIssueSchema` (caught this via a failing e2e 400 on issue creation, not via review). Added `packages/shared/src/label.ts` (`labelSchema`, `createLabelSchema`) and `issueListQuerySchema` (status/assigneeId/labelId/q/cursor/limit) in `issue.ts`; `q` is a plain case-insensitive Prisma `contains` on title+description — explicitly a placeholder, not the Postgres FTS search step (ADR-0006, still pending). Cross-project validation: `assigneeId` must be a workspace member, `parentId` must be an issue in the same project, `labelIds` must all belong to the same project — all 400 otherwise. No Prisma schema/migration changes were needed — `Issue`/`Label`/`Project.counter` already covered everything. **Verified (all PASSED):** `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3 pkgs via turbo), root `pnpm build` (3 pkgs), `pnpm --filter @workflo/api test` — 47/47 unit tests (26 prior + 15 new IssuesService incl. atomic counter allocation across two sequential creates, rank append, key parsing, cross-project assignee/parent/label validation, filter query building, cursor pagination + 6 new LabelsService incl. dup-name 409), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — 52/52 e2e (10 auth + 24 workspaces/projects + 18 new issues/labels: sequential key allocation proven end-to-end, GET by key incl. 404, status PATCH, assignee PATCH with cross-workspace-assignee 400, status/assigneeId/labelId list filters, label create+409-on-dup, label attach via PATCH labelIds, non-member 403 on list/read/create, DELETE issue and DELETE label). No new dependencies added (rank helper is hand-rolled, no `fractional-indexing` needed for this simple append-only step), so `pnpm install` was not run. Docker-compose left running throughout, untouched.

- **2026-07-02 — [BOARD-RANK]** Implemented the backend rank foundation for the Kanban board (backend only — `apps/web` untouched). Replaced the old append-only `apps/api/src/common/rank.ts` (`prev + "m"`) with a real fractional-indexing scheme shared between FE and BE: added the `fractional-indexing` npm package and a thin wrapper `packages/shared/src/rank.ts` — `rankBetween(before: string | null, after: string | null): string` (wraps `generateKeyBetween`, never returns a value equal to either neighbor, includes a comment marking where a future rebalance job would hook in — no background job built). `IssuesService.create` now computes the initial/append rank via `rankBetween(lastInStatus?.rank ?? null, null)` instead of the deleted helper. **New endpoint** `POST /issues/:key/move` (`apps/api/src/issues/issues.controller.ts` + `.service.ts`, same module — no new module) takes `moveIssueSchema = { status, beforeIssueId?, afterIssueId? }` (added to `packages/shared/src/issue.ts`): loads the named neighbors fresh from the DB (never trusts client ranks), 400s if either neighbor belongs to a different project or isn't in the target `status` column, computes the new rank via `rankBetween(afterRank, beforeRank)`, and updates `status`+`rank` in one `prisma.issue.update` call (atomic by construction — single statement). Omitting both neighbor ids places the issue at the end of the target column. AuthZ reuses the existing `issue:key` `WorkspaceMemberGuard` strategy unchanged (any member may move; non-member 403, unknown key 404). No Prisma schema/migration changes — `rank`/`status`/the `[projectId, status, rank]` index already existed. **Tooling fix**: `fractional-indexing` is ESM-only (`export const` at the package root), which broke ts-jest's default `transformIgnorePatterns: ["/node_modules/"]` for both `@workflo/shared` and `@workflo/api` (including e2e) — added a `transformIgnorePatterns` override (allowing `fractional-indexing` through the pnpm `.pnpm` nested store) to `packages/shared/package.json`, `apps/api/package.json`, and `apps/api/test/jest-e2e.json`; also added a minimal jest config + `test` script to `packages/shared/package.json` (it had no test runner before this step). **Verified (all PASSED):** `pnpm install` (added `fractional-indexing@4.0.0`, confirmed in `pnpm-lock.yaml`), `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3 pkgs via turbo), `pnpm --filter @workflo/shared test` — 6/6 unit tests (`rankBetween` first-key/append/prepend/between/never-equal-to-neighbor/repeated-midpoint-narrowing ordering), `pnpm --filter @workflo/api test` — 54/54 unit tests (47 prior + 2 updated `create` rank tests + 5 new `IssuesService.move` tests: rank strictly between neighbors, end-of-column with no neighbors, 400 on cross-project neighbor, 400 on wrong-status-column neighbor, 400 on unresolvable neighbor id, 404 on unresolvable move-target key), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — 59/59 e2e (52 prior + 7 new in `test/issues-move.e2e-spec.ts`: 3 issues created with strictly increasing ranks, move 3rd between 1st/2nd → list order 1,3,2, move TODO→IN_PROGRESS to end of column → both columns reflect the split, non-member 403, cross-project-neighbor 400, wrong-status-column-neighbor 400). Ran the e2e suite twice to confirm stability. Docker-compose left running throughout, untouched. Deviation: deleted the now-dead `apps/api/src/common/rank.ts` (superseded by the shared `rankBetween`, no remaining callers) rather than leaving unused code.

- **2026-07-02 — [BOARD-UI]** Implemented the frontend Kanban board + first real frontend foundations on `apps/web` (backend untouched). **API client** `src/lib/api.ts`: typed fetch wrapper (`/api/v1` base via the existing Vite proxy), attaches `Authorization: Bearer` from an in-memory token, always `credentials: "include"`, transparent single-retry 401→`/auth/refresh`→retry (dedupes concurrent refreshes into one in-flight promise), typed `ApiError { status, message, issues? }`. **Auth** `src/store/auth.store.ts` (Zustand): `accessToken`+`user` in memory only (never localStorage), `login`/`register`/`logout`/`bootstrap` (calls `/auth/refresh` once on mount; 401 just means logged-out, not an error state), wires itself into `api.ts` via a small `configureApiAuth` hook so the transport layer stays framework-agnostic. `src/routes/RequireAuth.tsx` renders nothing until bootstrap resolves, then redirects to `/login` if unauthenticated. **Routing** (`react-router-dom` added): `/login`, `/register` public; `/` (workspace/project picker) and `/projects/:projectId/board` protected; `src/components/TopBar.tsx` shows the user + logout. **Login/Register** (`src/pages/{Login,Register}Page.tsx`) validate client-side with the shared `loginSchema`/`registerSchema` (zero re-declared shapes), surface field errors + server errors (409 email-taken, 401 bad creds). **Workspace/project picker** `src/pages/HomePage.tsx` + `src/lib/queries.ts`: lists/creates workspaces and projects (`GET/POST /workspaces`, `GET/POST /projects`), minimal inline-create forms, click a project → its board. **Kanban board** (centerpiece) `src/features/board/`: `board.queries.ts` (TanStack Query: fetch `GET /projects/:id/issues`, `groupByStatus` sorted by `rank` ascending into TODO/IN_PROGRESS/DONE, `useCreateIssue`, `useMoveIssue`), `IssueCard.tsx` (key `WF-1`, title, priority badge, assignee-initial slot), `Column.tsx` (`@dnd-kit/sortable` `SortableContext` + droppable column body, create-issue input on the To Do column only), `BoardPage.tsx` (`DndContext` with `closestCorners` + `PointerSensor`, `DragOverlay` for the dragged card; on drop, computes the target column's before/after neighbors, calls the shared `rankBetween` for an instant optimistic local rank + status, patches the TanStack Query cache directly, then calls `POST /issues/:key/move { status, beforeIssueId?, afterIssueId? }` — server rank is authoritative and reconciles the cache on success; on error, rolls back to the pre-drag cache snapshot). `@dnd-kit/core`+`@dnd-kit/sortable`+`@dnd-kit/utilities` and `zustand` added as new deps (socket.io-client left as an unused stub per plan — real-time is next). No `packages/shared` changes were needed — every contract used (`authResponseSchema`, `loginSchema`/`registerSchema`, `workspaceSchema`/`createWorkspaceSchema`, `projectSchema`/`createProjectSchema`, `issueSchema`, `createIssueSchema`, `moveIssueSchema`, `rankBetween`) already existed and matched the live API exactly. **Verified (all PASSED, real output captured):** `pnpm install` (8 packages added, lockfile updated), `pnpm --filter @workflo/web run build` (`tsc -b && vite build` — 118 modules, clean), root `pnpm typecheck` (turbo, 3/3 packages, full turbo cache on repeat run). **Live smoke test**: created a local gitignored `apps/api/.env` (confirmed via `git check-ignore -v` — ignored by root `.gitignore` line 12) from `.env.example` pointing at the already-running docker Postgres (5434)/Redis (6380); booted the API and `vite dev` in the background (port 3000 was occupied on this dev machine by an unrelated local Grafana instance — same class of pre-existing local port conflict as the docker services, see 2026-07-01 log — so the API was smoke-tested on a temporary port 3050 with the Vite proxy temporarily repointed; both files were reverted to the spec's `:3000` immediately after testing and `git diff` confirms `vite.config.ts` has zero net changes). `curl http://localhost:5173/` → 200, real SPA HTML (`<title>Workflo</title>`, `#root`, `/src/main.tsx` module script). Full scripted happy path through the Vite proxy (`/api/v1/...`): register → 200 with `{accessToken, user}` → create workspace → 200 → create project (key `WF`) → 200 → create 2 issues → 200/200, ranks `a0`/`a1` → `GET /projects/:id/issues` → `{items:[...2 issues...], nextCursor:null}` confirming the exact shape `board.queries.ts` expects → `POST /issues/WF-1/move {status:"IN_PROGRESS"}` → 200, status flipped, rank preserved → `GET /auth/me` with bearer → 200 → `POST /auth/refresh` (cookie-only, no bearer) → 200 with a rotated `accessToken`. All dev server processes killed after testing (verified via `lsof`/`ps`); docker Postgres/Redis left running untouched. **Left for browser/orchestrator verification (explicitly out of headless scope):** actual drag-and-drop pointer interaction (dnd-kit sensor behavior, cross-column drag, drop-on-empty-column, visual `DragOverlay` rendering), the create-issue-in-To-Do form submission in a live DOM, login/register form UX and field-level error rendering, and general visual/spacing polish. Deviations: none functionally: the plan's "you MAY use `rankBetween` client-side for a snappy optimistic rank" was implemented as specified (server response still overwrites it on success).

- **2026-07-03 — [REALTIME]** Implemented the backend real-time layer per the ADR-0003-locked design (backend only — `apps/web` untouched; frontend socket integration is part B). **Transport**: `apps/api/src/realtime/realtime.gateway.ts` — `@nestjs/websockets` Socket.IO gateway, CORS to any origin with credentials (WEB_ORIGIN enforcement stays at the HTTP layer per existing pattern). **Handshake JWT auth**: reads `handshake.auth.token` (falls back to `Authorization: Bearer`), verifies with `JwtService.verifyAsync` against the same `JWT_ACCESS_SECRET` as the REST `JwtStrategy`; on missing/invalid token emits an `error` event and disconnects; on success stores `socket.data.userId`/`email`. Note: the transport-level handshake always completes first (Socket.IO connects, then our async `handleConnection` runs) — a bad token is rejected via an immediate app-level `error` + `disconnect(true)`, not a handshake-level refusal; e2e asserts on that disconnect. **Multi-pod fan-out**: `apps/api/src/realtime/redis-io.adapter.ts` (`RedisIoAdapter extends IoAdapter`, `@socket.io/redis-adapter` + `ioredis` pub/sub clients against `REDIS_URL`), installed in `main.ts` via `app.useWebSocketAdapter(...)` before `listen()` — confirmed it doesn't break the HTTP server (all existing e2e still pass). **Rooms + authz**: `joinProject { projectId }` loads the project → workspaceId → `WorkspaceMember` (same membership check shape as `WorkspaceMemberGuard`) — non-member or missing project → `error` event, no join; member → `socket.join('project:'+projectId)`. `leaveProject` leaves the room. Both re-broadcast presence. **Domain events**: added `@nestjs/event-emitter` (`EventEmitterModule.forRoot()`, global, in `AppModule`); `IssuesService` now takes `EventEmitter2` and emits `issue.created/updated/moved` (`{projectId, issue}`, AFTER the DB write) and `issue.deleted` (`{projectId, issueId}`) from `create`/`update`/`move`/`remove`. `apps/api/src/realtime/realtime.listener.ts` (`RealtimeListener`, `@OnEvent(...)`) broadcasts each to `project:{id}` via the gateway's `server`, whole-room including originator, as designed. **Presence**: on join/leave/disconnect, derives the room's distinct `userId`s via `server.in(room).fetchSockets()` (cross-pod-correct via the Redis adapter) and emits `presence.update {projectId, userIds}` to the room. **Shared**: new `packages/shared/src/realtime.ts` — `REALTIME_EVENTS`/`REALTIME_CLIENT_EVENTS` name constants, `issueEventPayloadSchema`/`issueDeletedEventPayloadSchema`/`presenceUpdatePayloadSchema` (Zod), `projectRoom(id)` helper — exported from `packages/shared/src/index.ts` for part B to reuse. No Prisma schema/migration changes. **Config fix (blocking, pre-existing, not introduced this session)**: `pnpm-workspace.yaml`'s `allowBuilds` had three unresolved placeholder values (`'@prisma/client'`, `'@prisma/engines'`, `esbuild`: literally `"set this to true or false"`) plus `'@nestjs/core': false` from an earlier partial `pnpm approve-builds` run — this blocked `pnpm install` outright (`ERR_PNPM_IGNORED_BUILDS`). Set all four to `true`, consistent with `onlyBuiltDependencies` already listing them and `argon2`'s existing `true`. **Deps added** (`apps/api/package.json`): `@nestjs/websockets`, `@nestjs/platform-socket.io`, `socket.io`, `@socket.io/redis-adapter`, `ioredis`, `@nestjs/event-emitter`; dev: `socket.io-client`. **Verified (all PASSED, real output):** `pnpm install` (24 packages added, clean after the `pnpm-workspace.yaml` fix), `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3/3 packages via turbo, incl. untouched `@workflo/web`), `pnpm --filter @workflo/api test` — **62/62 unit tests** (54 prior + 9 new `RealtimeGateway` specs: rejects missing token / rejects invalid token / accepts valid `handshake.auth.token` / falls back to `Authorization` header / `joinProject` rejects non-member / rejects unknown project / accepts member / rejects unauthenticated socket + 3 updated `IssuesService` specs asserting `issue.created`/`issue.moved`/`issue.deleted` are emitted with the right payload), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **65/65 e2e** (59 prior + 6 new in `test/realtime.e2e-spec.ts`, using real `socket.io-client` connections through the actual Redis-adapter-backed gateway: bad-token socket connects transport-wise then is force-disconnected; A+B join `project:{id}` and both receive `presence.update` with the correct distinct `userIds`; a non-member's `joinProject` gets an `error` and is proven to NOT receive a subsequent `issue.created` in the room; **both A's and B's real sockets receive `issue.created` with the correct issue payload including `updatedAt`** when A creates an issue via REST; **both receive `issue.moved`** with the correct payload when A moves an issue via REST; `leaveProject` updates presence to just the remaining member). Docker-compose Postgres/Redis left running throughout, untouched. Deviations: none from the locked design. Follow-up (explicitly out of scope per brief): no re-validation of the JWT for the life of an open socket (handshake-only auth, MVP-accepted trade-off — noted here per the brief's request).

_Next up: **Part B — frontend socket integration** on `apps/web` (single Socket.IO client using `packages/shared`'s new `REALTIME_EVENTS`/payload types; connect with the stored access token; `joinProject`/`leaveProject` on board mount/unmount; patch the TanStack Query issues cache directly on `issue.created/updated/moved/deleted` with last-writer-wins via `updatedAt`, no full refetch; presence indicator using `presence.update`; reconnect resync — on reconnect, refetch the board once to reconcile any missed events since sockets don't buffer/replay). Then issue-detail page + comments (with `@mentions`), which will need a new `comment.added` real-time event added to this same layer._
