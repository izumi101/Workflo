# CLAUDE.md — Workflo Operating Manual

> This file is auto-loaded by Claude Code at the start of every session.
> It is the **single source of truth** for how work is done on Workflo.
> Claude MUST read the Progress Log before acting and MUST append to it after every meaningful step.

---

## 1. What Workflo Is

Workflo is a **Jira alternative** — the goal is to build a project/issue tracker that beats Jira on:
- **Speed** — instant, keyboard-first UI (Linear-style), no slow page loads.
- **Simplicity** — no JQL barrier, no heavy admin/permission schemes to configure.
- **Real-time** — live collaboration (presence, live board updates) built in from day one.
- **Low notification noise** — smart, filterable notifications.

We are NOT trying to replicate every Jira feature. We deliberately cut scope (see §5).

---

## 2. Tech Stack (locked)

| Layer | Choice | Notes |
|-------|--------|-------|
| Frontend | **React 18 + TypeScript + Vite** | SPA. State: TanStack Query + Zustand. UI: keyboard-first. |
| Backend | **NestJS (Node + TypeScript)** | Modular monolith. |
| ORM | **Prisma** | Type-safe DB access. |
| Database | **PostgreSQL** | Primary datastore. Full-text search via Postgres FTS (no Elastic in MVP). |
| Cache / PubSub | **Redis** | Cache, Socket.IO adapter, BullMQ queues. |
| Real-time | **Socket.IO** | Redis adapter for horizontal scale. |
| Queues | **BullMQ** | Notifications, async automations. |
| Repo | **pnpm monorepo (Turborepo)** | `apps/web`, `apps/api`, `packages/shared`. |
| Shared types | **`packages/shared`** | Zod schemas + TS types shared FE/BE for end-to-end type safety. |

Full rationale in [docs/architecture.md](docs/architecture.md) and [docs/adr/](docs/adr/).

---

## 3. Model Routing (MANDATORY)

Route by **task ambiguity + blast radius**, not a fixed "the big model always plans" rule. Goal: don't burn Fable/Opus on easy work — save them for genuinely murky steps, and escalate *up* only when a step earns it.

| Step type | Plan on | Implement on |
|-----------|---------|--------------|
| **Trivial / clear** — touches ≤2 modules AND the approach is already specified in the roadmap / an ADR / a spec (e.g. an endpoint per spec, one more component) | Sonnet 5 plans itself | **Sonnet 5** |
| **Ambiguous / cross-cutting** — touches >2 modules, OR the approach isn't written down yet, OR it adds a new cross-cutting concern | **Opus 4.8** (main session or a planning sub-agent) | **Sonnet 5** |
| **Genuinely hard planning** — Opus honestly tried and is circling (can't reconcile the constraints) | **Fable 5** — one pass, not iterative | **Sonnet 5** |

Code + tests are always written by **Sonnet 5** (dispatch via the `Agent` tool with `model: "sonnet"`), never directly on Opus.

**UX/UI design → Fable 5, but only where it earns it (user rule, refined 2026-07-04):** **substantial or novel** user-facing design — the design system (2.4), hero/complex flows, new interaction paradigms, the differentiating features — is **designed on Fable 5**, then **implemented on Sonnet 5**, then the Opus verification gate. Do **NOT** spend Fable on small, derivative UI that just follows existing patterns/tokens (one more dropdown/list/form/bell) — Sonnet designs+builds those itself, or Opus does a quick design pass. Fable burns limits; use it where the design thinking is genuinely hard, not on every component. The gut check: *"is there real, novel design to figure out here?"* — if no, it's not a Fable job. (Learned: the notification bell was routed to Fable but was too derivative to justify it.)

**Foundation / architecture review:** default to a **fresh Opus adversarial pass** ("where does this break at scale? what's the weakest assumption?") — cheap, catches ~90%. Escalate a review to **Fable** only when the foundation is genuinely non-standard AND everything else will sit on top of it — one pass, before committing.

**Verification gate (never skipped):** whoever wrote the code, the orchestrator (Opus) verifies with a real run (build / typecheck / tests) before commit. Autonomy scales the *planning depth*, not the right to skip verification — Sonnet-alone has produced false "done" reports and missed a real bug here, so a tight self-contained brief + an Opus check stays mandatory.

Rule of thumb: **spend the expensive model only where the thinking is expensive.**

---

## 4. Working Agreement (how Claude must behave)

1. **Read the Progress Log (§8) first.** Never re-decide something already decided; never redo done work.
2. **Log every meaningful step** in §8 with date + what changed + why. This is the "don't forget / don't mess up" contract.
3. **Route models by task ambiguity / blast radius, not a fixed default** (see §3) — escalate to an Opus/Fable planner only when a step earns it; never skip the Opus verification gate before commit.
4. **Small, verifiable steps.** One coherent change at a time; verify before moving on.
5. **Keep shared contracts in `packages/shared`.** FE and BE must never drift on types — change the Zod schema, both sides follow.
6. **Do not expand MVP scope** (§5) without the user explicitly approving. Park ideas in §7 Backlog.
7. **Ask before irreversible/outward-facing actions** (deploys, deleting data, public releases).
8. **Conventional Commits** for git messages. Branch off `main`; never commit straight to `main` for feature work once code exists.

---

## 5. MVP Scope (locked)

**In:**
- Auth: email/password + Google OAuth. Single workspace role model (owner/member).
- Workspace → Project → Issue data model. Issue types: Task, Bug (+ Epic as a lightweight grouping).
- Issue fields: title, description, status, assignee, reporter, priority, labels, due date.
- Fixed workflow: `To Do → In Progress → Done`.
- Kanban board with drag & drop (status change + rank ordering).
- Backlog / list view with filters.
- Issue detail page: comments + `@mentions`.
- **Real-time** updates over WebSocket (board, issue, comments, presence).
- Fast simple search (title/description/assignee) — **no JQL**.

**Deliberately OUT of MVP** (see roadmap phases 4–5):
Sprints & full Scrum mechanics · Epic-portfolio/Roadmap timeline · Automation rules · Custom workflows · GitHub/Slack integrations · Time tracking/worklogs · Service desk/SLA · SSO/audit logs/granular permission schemes · Attachments (may pull earlier if cheap).

---

## 6. Roadmap to Production (detailed — single source of truth for "what's next")

> Each numbered item ≈ one feature-step of the size we've been shipping (plan → Sonnet → verify → PR).
> Shipped so far: auth, workspaces/projects+authZ, issues+labels, board (rank BE + dnd UI), realtime (BE+FE).

### Phase 0 — MVP completion *(current)*
- 0.1 Issue detail page + comments + `@mentions` (BE + FE, incl. `comment.added` realtime event) — biggest remaining chunk.
- 0.2 Backlog / list view with filters (FE over the existing list API).
- 0.3 Search: Postgres FTS endpoint (`tsvector`, replaces the `contains` placeholder, ADR-0006) + search UI. Still no JQL.
- 0.4 Notifications: mention/assign → Notification rows (BullMQ) + minimal notification list UI.
- 0.5 Tech-queue burn-down: realtime payload/schema alignment (ADR-0004), board reverse-drag animation fix, dnd `KeyboardSensor`, socket CORS → `WEB_ORIGIN`.
- **Exit:** every §5 "In" item works end-to-end.

### Phase 1 — Dogfooding (private beta)
- 1.1 CI: GitHub Actions — lint / typecheck / unit / e2e (dockerized PG+Redis) on every PR.
- 1.2 Deploy: hosting (Fly/Railway/VPS) + managed Postgres & Redis + domain + **HTTPS** (Secure cookies become real).
- 1.3 Real secrets: Google OAuth creds, strong rotated JWT secrets, refresh-token cleanup job.
- 1.4 Observability: Sentry (api+web), structured logs w/ request IDs, uptime monitor.
- 1.5 Postgres backups + one restore drill.
- 1.6 Move Workflo's own task tracking INTO Workflo (dogfood loop starts).
- **Exit:** we use Workflo daily to build Workflo.

### Phase 2 — Public beta (~10–50 users)
- 2.1 Transactional email (provider + templates): verification, password reset.
- 2.2 Workspace invites by email + first-run onboarding.
- 2.3 Notification center UX — the "low noise" pitch: grouping, digest, per-project/issue mute.
- 2.4 **Design system & UI overhaul** — dress the skeleton: tokens, component library, empty/loading/error states, micro-animations, avatars, density. Use `ui-ux-pro-max` / `frontend-design` skills. Queued visual bugs fold in here.
- 2.5 Performance under load: index review, rank rebalancer job, k6 load test, N+1 audit.
- 2.6 Product metrics: activation, retention, time-to-first-action.
- **Exit:** external users, real retention data.

### Phase 3 — V1 (production release) ← the "prod-ready" line
- 3.1 Beta findings burn-down + API freeze (`/api/v1` semantics locked).
- 3.2 Billing (Stripe) — only if monetizing at V1.
- 3.3 Legal minimum: ToS/privacy, full account+data deletion.
- 3.4 Docs: user guide + API reference.
- **Exit:** public, stable, supported product.

### Phase 4 — Differentiation (post-V1 growth bets)
- ⌘K command palette + keyboard-first everything (core pitch).
- Smart search: filter chips, saved views — power without JQL.
- **Import-from-Jira** — THE adoption lever for a Jira competitor.
- Git integration (GitHub): PR/commit ↔ issue linking, auto-transitions.
- Epics UI, attachments (from §7).

### Phase 5 — Scale / enterprise
- SSO/SAML, granular permissions, audit logs.
- Slack integration, public API tokens + webhooks.
- Horizontal scale work (already architected: stateless pods + Redis adapter).

### 6.1 After the roadmap — the operating loop (plan for future me)
Once Phase 5 ships, the line becomes a **loop**. Operate like this:
1. **Metrics decide, not vibes** — weekly activation/retention/usage review → pick next bets → re-prioritize §7 (don't just append).
2. **Feedback pipeline** — user reports become Workflo issues in Workflo (dogfood); weekly triage.
3. **Tech-debt budget** — ~20% of every cycle: dependency majors (Node/Nest/Prisma/React), security patches, audit queue.
4. **Security posture** — periodic `security-reviewer` audit; pen-test before the first enterprise deal; incident runbook.
5. **Release discipline** — semver the API, changelog, migration policy, feature flags for risky bets.
6. **Growth experiments** — pricing tiers, project templates, integrations marketplace; measure every bet against retention.
7. **Honest checkpoints (kill/pivot criteria)** — weak beta retention ⇒ fix positioning/UX before building enterprise; never scale what doesn't retain.
8. **Process survives the roadmap** — model routing (§3), CLAUDE.md as SSOT, append-only log (§8), verification gate before every merge.

---

## 7. Backlog / Parking Lot

_(Ideas that are out of current scope — do not build without approval.)_
- Sprints / Scrum boards
- Timeline / roadmap view
- Automation rules engine
- Attachments & rich media in comments

---

## 8. Progress Log (APPEND-ONLY — newest at bottom)

> Format: `YYYY-MM-DD — [PHASE] what changed — why / decision`

- **2026-07-01 — [SETUP]** Created GitHub repo `izumi101/Workflo` (public), initialized git, pushed initial commit to `main`.
- **2026-07-01 — [PLANNING]** Locked stack (React + NestJS + Postgres + Prisma + Redis), MVP scope, and post-MVP roadmap with user. Saved to memory.
- **2026-07-01 — [PLANNING]** Established model routing rule: Opus 4.8/Fable for planning, Sonnet 5 for code & tests.
- **2026-07-01 — [ARCHITECTURE]** Wrote full architecture: `CLAUDE.md`, `docs/architecture.md`, ADR-0001..0006, data model sketch, monorepo layout. Used `architecture-designer` skill. No implementation code yet (deferred to Sonnet).
- **2026-07-01 — [PLANNING]** Model routing clarified by user: **Fable 5 is the default planner, Opus 4.8 is fallback** when Fable is unavailable. Updated §3 + memory. Decided NOT to run `ln-100-documents-pipeline` now (overlaps/would overwrite these docs; revisit at beta with a tracker).
- **2026-07-01 — [ARCHITECTURE]** Presented container architecture diagram to user for review before scaffolding. Awaiting go-ahead to scaffold.
- **2026-07-01 — [SCAFFOLD]** Scaffolded pnpm+turbo monorepo: packages/shared (zod schemas), apps/api (NestJS + Prisma schema + /api/v1/health), apps/web (Vite+React+TanStack Query, health round-trip). Verified: pnpm install, shared build/typecheck, `prisma validate`, api build, web build, root `pnpm build`/`pnpm typecheck` via turbo, and a live boot of the API (`PORT=3099`) returning 200 from `GET /api/v1/health` with no DB/Redis running.
- **2026-07-01 — [AUTH]** Implemented the Auth module per ADR-0005: email/password (argon2id) + Google OAuth (Passport), JWT access token (in JSON body) + opaque rotating refresh token (sha256-hashed at rest, httpOnly+SameSite=strict cookie scoped to `/api/v1/auth`, `Secure` in prod) with **reuse detection** (a reused/revoked/expired refresh token revokes the whole `family` and 401s). Routes under `/api/v1/auth`: register(409 on dup)/login/refresh/logout/me(JwtAuthGuard)/google/google/callback. Added: root `docker-compose.yml` (postgres:16 + redis:7, healthchecks, named volume), `.env.example` split into `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` + `WEB_ORIGIN` (+ `apps/api/.env.example`), fail-fast env validation for the new secrets, `RefreshToken` Prisma model + **applied** initial migration `20260701180835_init` (all models), shared auth zod schemas (`registerSchema`/`loginSchema`/`authUserSchema`/`authResponseSchema` — never expose passwordHash), `@nestjs/throttler` global guard (5/min on login+register), `cookie-parser`, CORS locked to `WEB_ORIGIN`. **Verified (all PASSED):** `pnpm install`, shared build, `nest build`, root `pnpm typecheck` (3 pkgs), 11 unit tests (AuthService, incl. reuse-detection), and 10 supertest **e2e against a real Postgres** (register→login→me(bearer)→refresh-rotation→reuse-detection→logout), plus a live HTTP curl round-trip and a fail-fast boot check. New deps: argon2 ^0.44, @nestjs/jwt ^10.2, @nestjs/passport ^10.0.3, passport ^0.7, passport-jwt ^4, passport-google-oauth20 ^2, @nestjs/throttler ^6.5, cookie-parser ^1.4 (+types, jest/ts-jest/supertest/@nestjs/testing). Docker note: local host ports 5432/6379 were occupied by an unrelated `eventhub_*` stack on this dev machine, so the **committed** `docker-compose.yml` and both `.env.example` files map Postgres to host port **5434** and Redis to host port **6380** (container-internal ports unchanged at 5432/6379) — the migration and e2e run were against these real, docker-composed services. On a clean machine with 5432/6379 free, remap back if desired. Follow-ups before prod: real Google OAuth creds, HTTPS so `Secure` cookies apply, strong rotated JWT secrets, and consider a refresh-token cleanup job + optional access-token denylist.

- **2026-07-01 — [AUTH]** Review found a validation defect: `register`/`login` used `@UsePipes(ZodValidationPipe)` with no `@Body()` param, so request-body validation was a silent no-op (malformed input reached the service; invalid register returned 500, not 400). Fix (Sonnet): rebound to `@Body(new ZodValidationPipe(schema))` and hardened the pipe to only run when `metadata.type === 'body'`; added 4 e2e regression cases (bad email / short password / missing name / malformed login → 400). **Re-verified by orchestrator against live docker Postgres (5434): `nest build`, `pnpm typecheck`, 11/11 unit, 14/14 e2e — all PASSED.**

- **2026-07-02 — [PROCESS]** Revised model routing (§3) from "the big model always plans" to **tiered escalation by ambiguity/blast-radius**: Sonnet plans+implements clear ≤2-module steps; Opus plans cross-cutting/undocumented steps; Fable only when Opus is genuinely circling. Foundation review now defaults to a fresh Opus adversarial pass (not a paid Fable pass). Opus verification gate before commit stays mandatory. Rationale (user): stop paying premium models on trivial work. Updated §3, §4, and memory.

- **2026-07-02 — [WORKSPACES/PROJECTS]** Implemented Workspaces + Projects CRUD with the project's first authZ layer, on top of the existing JWT auth. New `apps/api/src/authz/` module: `WorkspaceMemberGuard` (resolves the target workspace via a `@ResolveWorkspaceFrom(...)` strategy — `param:id`/`param:workspaceId`/`body:workspaceId`/`query:workspaceId`/`project:id` (loads the project then uses its workspaceId) — 404s if the workspace/project doesn't exist, 403s if the caller has no `WorkspaceMember` row, else attaches `{ workspaceId, role }` to the request), `RolesGuard` + `@Roles(...)` decorator (enforces role against the attached context), `@WorkspaceContext()` param decorator. Guards run after `JwtAuthGuard`, mirroring the existing auth-module style (Zod validation via `@Body(new ZodValidationPipe(schema))`, never `@UsePipes` without `@Body()`). `apps/api/src/workspaces/`: `POST/GET /workspaces`, `GET/PATCH/DELETE /workspaces/:id`, `GET/POST /workspaces/:id/members`, `PATCH/DELETE /workspaces/:id/members/:userId` — create wraps workspace+OWNER-membership creation in one `$transaction`, slug is auto-derived from name with a uniqueness-suffix loop, add-member 404s on unknown email / 409s on existing member, and both demote-last-owner and remove-last-owner are blocked with 400 (owner count checked via `workspaceMember.count`). `apps/api/src/projects/`: `POST/GET /projects` (create/list, `?workspaceId=`), `GET/PATCH/DELETE /projects/:id` — key uniqueness is per-workspace (`@@unique([workspaceId,key])`, 409 on conflict), delete is OWNER-only, create/read/update are any member. Shared contracts: new `packages/shared/src/workspace.ts` (`workspaceSchema`, `createWorkspaceSchema`, `updateWorkspaceSchema`, `workspaceMemberSchema`, `addMemberSchema`, `updateMemberRoleSchema`, reusing `roleSchema`/`authUserSchema`), and `packages/shared/src/issue.ts` gained `updateProjectSchema` plus a stricter `createProjectSchema.key` regex (uppercase, starts with a letter). No Prisma schema/migration changes were needed — existing `User`/`Workspace`/`WorkspaceMember`/`Project` models already covered everything. **Verified (all PASSED):** `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3 pkgs via turbo), `pnpm --filter @workflo/api test` — 26/26 unit tests (11 auth + 9 WorkspacesService incl. create-makes-owner/slug-uniqueness/add-member 404+409/last-owner protection + 6 ProjectsService incl. key-conflict), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — 34/34 e2e (10 auth + 24 new: two-user flow proving non-member 403, workspace/project 404, add-member 404/409, member can read+patch-project but is 403'd on owner-only workspace-patch/project-delete/add-member, owner actions succeed, project key 409, last-owner demote/remove blocked with 400, ownership transferable then original owner safely demotable). Ran the e2e suite twice to confirm stability; docker-compose DB/Redis left running throughout, untouched. No new dependencies added, so `pnpm install` was not run. Deviation from brief: none functionally — the `@ResolveWorkspaceFrom` strategy decorator was an implementation detail (not explicitly specified) needed to make one `WorkspaceMemberGuard` work across all five resolution shapes cleanly.

- **2026-07-02 — [ISSUES]** Implemented Issues CRUD + minimal Labels on top of the Workspaces/Projects authZ layer. `apps/api/src/issues/`: `POST/GET /projects/:id/issues`, `GET/PATCH/DELETE /issues/:key`. `apps/api/src/labels/`: `POST/GET /projects/:id/labels`, `DELETE /labels/:id` (any workspace member may manage labels — no owner restriction). **Human key allocation**: `number` is allocated by an atomic `prisma.project.update({ data: { counter: { increment: 1 } } })` inside the same `$transaction` as the issue insert, using the returned counter value — verified sequential (`-1` then `-2`) both in a mocked unit test and against real Postgres in e2e. **Rank**: kept deliberately simple per plan — `apps/api/src/common/rank.ts` appends a new issue after the current last-in-status-column issue via a plain lexicographic string (`prev + "m"`, no new npm dependency); PATCH can accept a client-supplied `rank` string verbatim. Full LexoRank midpoint insertion for drag & drop is deferred (see Next up). **AuthZ**: extended `@ResolveWorkspaceFrom` with two new strategies on the existing `WorkspaceMemberGuard` — `issue:key` (parses `WF-123`, joins through all same-keyed projects to the issue, since `Project.key` is only unique *within* a workspace, not globally) and `label:id` (label → its project → workspaceId); both 404 on not-found, 403 on non-member. **Query validation gap found+fixed**: `ZodValidationPipe` was body-only by design (existing safety net), so list-query filters were silently unvalidated — added a sibling `ZodQueryValidationPipe` in the same file for `@Query(...)`. **Shared contract fix**: `createIssueSchema` originally required `projectId` in the body (leftover from the pre-Issues scaffold), which is wrong since the route supplies it — removed it from `createIssueSchema` (caught this via a failing e2e 400 on issue creation, not via review). Added `packages/shared/src/label.ts` (`labelSchema`, `createLabelSchema`) and `issueListQuerySchema` (status/assigneeId/labelId/q/cursor/limit) in `issue.ts`; `q` is a plain case-insensitive Prisma `contains` on title+description — explicitly a placeholder, not the Postgres FTS search step (ADR-0006, still pending). Cross-project validation: `assigneeId` must be a workspace member, `parentId` must be an issue in the same project, `labelIds` must all belong to the same project — all 400 otherwise. No Prisma schema/migration changes were needed — `Issue`/`Label`/`Project.counter` already covered everything. **Verified (all PASSED):** `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3 pkgs via turbo), root `pnpm build` (3 pkgs), `pnpm --filter @workflo/api test` — 47/47 unit tests (26 prior + 15 new IssuesService incl. atomic counter allocation across two sequential creates, rank append, key parsing, cross-project assignee/parent/label validation, filter query building, cursor pagination + 6 new LabelsService incl. dup-name 409), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — 52/52 e2e (10 auth + 24 workspaces/projects + 18 new issues/labels: sequential key allocation proven end-to-end, GET by key incl. 404, status PATCH, assignee PATCH with cross-workspace-assignee 400, status/assigneeId/labelId list filters, label create+409-on-dup, label attach via PATCH labelIds, non-member 403 on list/read/create, DELETE issue and DELETE label). No new dependencies added (rank helper is hand-rolled, no `fractional-indexing` needed for this simple append-only step), so `pnpm install` was not run. Docker-compose left running throughout, untouched.

- **2026-07-02 — [BOARD-RANK]** Implemented the backend rank foundation for the Kanban board (backend only — `apps/web` untouched). Replaced the old append-only `apps/api/src/common/rank.ts` (`prev + "m"`) with a real fractional-indexing scheme shared between FE and BE: added the `fractional-indexing` npm package and a thin wrapper `packages/shared/src/rank.ts` — `rankBetween(before: string | null, after: string | null): string` (wraps `generateKeyBetween`, never returns a value equal to either neighbor, includes a comment marking where a future rebalance job would hook in — no background job built). `IssuesService.create` now computes the initial/append rank via `rankBetween(lastInStatus?.rank ?? null, null)` instead of the deleted helper. **New endpoint** `POST /issues/:key/move` (`apps/api/src/issues/issues.controller.ts` + `.service.ts`, same module — no new module) takes `moveIssueSchema = { status, beforeIssueId?, afterIssueId? }` (added to `packages/shared/src/issue.ts`): loads the named neighbors fresh from the DB (never trusts client ranks), 400s if either neighbor belongs to a different project or isn't in the target `status` column, computes the new rank via `rankBetween(afterRank, beforeRank)`, and updates `status`+`rank` in one `prisma.issue.update` call (atomic by construction — single statement). Omitting both neighbor ids places the issue at the end of the target column. AuthZ reuses the existing `issue:key` `WorkspaceMemberGuard` strategy unchanged (any member may move; non-member 403, unknown key 404). No Prisma schema/migration changes — `rank`/`status`/the `[projectId, status, rank]` index already existed. **Tooling fix**: `fractional-indexing` is ESM-only (`export const` at the package root), which broke ts-jest's default `transformIgnorePatterns: ["/node_modules/"]` for both `@workflo/shared` and `@workflo/api` (including e2e) — added a `transformIgnorePatterns` override (allowing `fractional-indexing` through the pnpm `.pnpm` nested store) to `packages/shared/package.json`, `apps/api/package.json`, and `apps/api/test/jest-e2e.json`; also added a minimal jest config + `test` script to `packages/shared/package.json` (it had no test runner before this step). **Verified (all PASSED):** `pnpm install` (added `fractional-indexing@4.0.0`, confirmed in `pnpm-lock.yaml`), `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3 pkgs via turbo), `pnpm --filter @workflo/shared test` — 6/6 unit tests (`rankBetween` first-key/append/prepend/between/never-equal-to-neighbor/repeated-midpoint-narrowing ordering), `pnpm --filter @workflo/api test` — 54/54 unit tests (47 prior + 2 updated `create` rank tests + 5 new `IssuesService.move` tests: rank strictly between neighbors, end-of-column with no neighbors, 400 on cross-project neighbor, 400 on wrong-status-column neighbor, 400 on unresolvable neighbor id, 404 on unresolvable move-target key), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — 59/59 e2e (52 prior + 7 new in `test/issues-move.e2e-spec.ts`: 3 issues created with strictly increasing ranks, move 3rd between 1st/2nd → list order 1,3,2, move TODO→IN_PROGRESS to end of column → both columns reflect the split, non-member 403, cross-project-neighbor 400, wrong-status-column-neighbor 400). Ran the e2e suite twice to confirm stability. Docker-compose left running throughout, untouched. Deviation: deleted the now-dead `apps/api/src/common/rank.ts` (superseded by the shared `rankBetween`, no remaining callers) rather than leaving unused code.

- **2026-07-02 — [BOARD-UI]** Implemented the frontend Kanban board + first real frontend foundations on `apps/web` (backend untouched). **API client** `src/lib/api.ts`: typed fetch wrapper (`/api/v1` base via the existing Vite proxy), attaches `Authorization: Bearer` from an in-memory token, always `credentials: "include"`, transparent single-retry 401→`/auth/refresh`→retry (dedupes concurrent refreshes into one in-flight promise), typed `ApiError { status, message, issues? }`. **Auth** `src/store/auth.store.ts` (Zustand): `accessToken`+`user` in memory only (never localStorage), `login`/`register`/`logout`/`bootstrap` (calls `/auth/refresh` once on mount; 401 just means logged-out, not an error state), wires itself into `api.ts` via a small `configureApiAuth` hook so the transport layer stays framework-agnostic. `src/routes/RequireAuth.tsx` renders nothing until bootstrap resolves, then redirects to `/login` if unauthenticated. **Routing** (`react-router-dom` added): `/login`, `/register` public; `/` (workspace/project picker) and `/projects/:projectId/board` protected; `src/components/TopBar.tsx` shows the user + logout. **Login/Register** (`src/pages/{Login,Register}Page.tsx`) validate client-side with the shared `loginSchema`/`registerSchema` (zero re-declared shapes), surface field errors + server errors (409 email-taken, 401 bad creds). **Workspace/project picker** `src/pages/HomePage.tsx` + `src/lib/queries.ts`: lists/creates workspaces and projects (`GET/POST /workspaces`, `GET/POST /projects`), minimal inline-create forms, click a project → its board. **Kanban board** (centerpiece) `src/features/board/`: `board.queries.ts` (TanStack Query: fetch `GET /projects/:id/issues`, `groupByStatus` sorted by `rank` ascending into TODO/IN_PROGRESS/DONE, `useCreateIssue`, `useMoveIssue`), `IssueCard.tsx` (key `WF-1`, title, priority badge, assignee-initial slot), `Column.tsx` (`@dnd-kit/sortable` `SortableContext` + droppable column body, create-issue input on the To Do column only), `BoardPage.tsx` (`DndContext` with `closestCorners` + `PointerSensor`, `DragOverlay` for the dragged card; on drop, computes the target column's before/after neighbors, calls the shared `rankBetween` for an instant optimistic local rank + status, patches the TanStack Query cache directly, then calls `POST /issues/:key/move { status, beforeIssueId?, afterIssueId? }` — server rank is authoritative and reconciles the cache on success; on error, rolls back to the pre-drag cache snapshot). `@dnd-kit/core`+`@dnd-kit/sortable`+`@dnd-kit/utilities` and `zustand` added as new deps (socket.io-client left as an unused stub per plan — real-time is next). No `packages/shared` changes were needed — every contract used (`authResponseSchema`, `loginSchema`/`registerSchema`, `workspaceSchema`/`createWorkspaceSchema`, `projectSchema`/`createProjectSchema`, `issueSchema`, `createIssueSchema`, `moveIssueSchema`, `rankBetween`) already existed and matched the live API exactly. **Verified (all PASSED, real output captured):** `pnpm install` (8 packages added, lockfile updated), `pnpm --filter @workflo/web run build` (`tsc -b && vite build` — 118 modules, clean), root `pnpm typecheck` (turbo, 3/3 packages, full turbo cache on repeat run). **Live smoke test**: created a local gitignored `apps/api/.env` (confirmed via `git check-ignore -v` — ignored by root `.gitignore` line 12) from `.env.example` pointing at the already-running docker Postgres (5434)/Redis (6380); booted the API and `vite dev` in the background (port 3000 was occupied on this dev machine by an unrelated local Grafana instance — same class of pre-existing local port conflict as the docker services, see 2026-07-01 log — so the API was smoke-tested on a temporary port 3050 with the Vite proxy temporarily repointed; both files were reverted to the spec's `:3000` immediately after testing and `git diff` confirms `vite.config.ts` has zero net changes). `curl http://localhost:5173/` → 200, real SPA HTML (`<title>Workflo</title>`, `#root`, `/src/main.tsx` module script). Full scripted happy path through the Vite proxy (`/api/v1/...`): register → 200 with `{accessToken, user}` → create workspace → 200 → create project (key `WF`) → 200 → create 2 issues → 200/200, ranks `a0`/`a1` → `GET /projects/:id/issues` → `{items:[...2 issues...], nextCursor:null}` confirming the exact shape `board.queries.ts` expects → `POST /issues/WF-1/move {status:"IN_PROGRESS"}` → 200, status flipped, rank preserved → `GET /auth/me` with bearer → 200 → `POST /auth/refresh` (cookie-only, no bearer) → 200 with a rotated `accessToken`. All dev server processes killed after testing (verified via `lsof`/`ps`); docker Postgres/Redis left running untouched. **Left for browser/orchestrator verification (explicitly out of headless scope):** actual drag-and-drop pointer interaction (dnd-kit sensor behavior, cross-column drag, drop-on-empty-column, visual `DragOverlay` rendering), the create-issue-in-To-Do form submission in a live DOM, login/register form UX and field-level error rendering, and general visual/spacing polish. Deviations: none functionally: the plan's "you MAY use `rankBetween` client-side for a snappy optimistic rank" was implemented as specified (server response still overwrites it on success).

- **2026-07-03 — [REALTIME]** Implemented the backend real-time layer per the ADR-0003-locked design (backend only — `apps/web` untouched; frontend socket integration is part B). **Transport**: `apps/api/src/realtime/realtime.gateway.ts` — `@nestjs/websockets` Socket.IO gateway, CORS to any origin with credentials (WEB_ORIGIN enforcement stays at the HTTP layer per existing pattern). **Handshake JWT auth**: reads `handshake.auth.token` (falls back to `Authorization: Bearer`), verifies with `JwtService.verifyAsync` against the same `JWT_ACCESS_SECRET` as the REST `JwtStrategy`; on missing/invalid token emits an `error` event and disconnects; on success stores `socket.data.userId`/`email`. Note: the transport-level handshake always completes first (Socket.IO connects, then our async `handleConnection` runs) — a bad token is rejected via an immediate app-level `error` + `disconnect(true)`, not a handshake-level refusal; e2e asserts on that disconnect. **Multi-pod fan-out**: `apps/api/src/realtime/redis-io.adapter.ts` (`RedisIoAdapter extends IoAdapter`, `@socket.io/redis-adapter` + `ioredis` pub/sub clients against `REDIS_URL`), installed in `main.ts` via `app.useWebSocketAdapter(...)` before `listen()` — confirmed it doesn't break the HTTP server (all existing e2e still pass). **Rooms + authz**: `joinProject { projectId }` loads the project → workspaceId → `WorkspaceMember` (same membership check shape as `WorkspaceMemberGuard`) — non-member or missing project → `error` event, no join; member → `socket.join('project:'+projectId)`. `leaveProject` leaves the room. Both re-broadcast presence. **Domain events**: added `@nestjs/event-emitter` (`EventEmitterModule.forRoot()`, global, in `AppModule`); `IssuesService` now takes `EventEmitter2` and emits `issue.created/updated/moved` (`{projectId, issue}`, AFTER the DB write) and `issue.deleted` (`{projectId, issueId}`) from `create`/`update`/`move`/`remove`. `apps/api/src/realtime/realtime.listener.ts` (`RealtimeListener`, `@OnEvent(...)`) broadcasts each to `project:{id}` via the gateway's `server`, whole-room including originator, as designed. **Presence**: on join/leave/disconnect, derives the room's distinct `userId`s via `server.in(room).fetchSockets()` (cross-pod-correct via the Redis adapter) and emits `presence.update {projectId, userIds}` to the room. **Shared**: new `packages/shared/src/realtime.ts` — `REALTIME_EVENTS`/`REALTIME_CLIENT_EVENTS` name constants, `issueEventPayloadSchema`/`issueDeletedEventPayloadSchema`/`presenceUpdatePayloadSchema` (Zod), `projectRoom(id)` helper — exported from `packages/shared/src/index.ts` for part B to reuse. No Prisma schema/migration changes. **Config fix (blocking, pre-existing, not introduced this session)**: `pnpm-workspace.yaml`'s `allowBuilds` had three unresolved placeholder values (`'@prisma/client'`, `'@prisma/engines'`, `esbuild`: literally `"set this to true or false"`) plus `'@nestjs/core': false` from an earlier partial `pnpm approve-builds` run — this blocked `pnpm install` outright (`ERR_PNPM_IGNORED_BUILDS`). Set all four to `true`, consistent with `onlyBuiltDependencies` already listing them and `argon2`'s existing `true`. **Deps added** (`apps/api/package.json`): `@nestjs/websockets`, `@nestjs/platform-socket.io`, `socket.io`, `@socket.io/redis-adapter`, `ioredis`, `@nestjs/event-emitter`; dev: `socket.io-client`. **Verified (all PASSED, real output):** `pnpm install` (24 packages added, clean after the `pnpm-workspace.yaml` fix), `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (3/3 packages via turbo, incl. untouched `@workflo/web`), `pnpm --filter @workflo/api test` — **62/62 unit tests** (54 prior + 9 new `RealtimeGateway` specs: rejects missing token / rejects invalid token / accepts valid `handshake.auth.token` / falls back to `Authorization` header / `joinProject` rejects non-member / rejects unknown project / accepts member / rejects unauthenticated socket + 3 updated `IssuesService` specs asserting `issue.created`/`issue.moved`/`issue.deleted` are emitted with the right payload), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **65/65 e2e** (59 prior + 6 new in `test/realtime.e2e-spec.ts`, using real `socket.io-client` connections through the actual Redis-adapter-backed gateway: bad-token socket connects transport-wise then is force-disconnected; A+B join `project:{id}` and both receive `presence.update` with the correct distinct `userIds`; a non-member's `joinProject` gets an `error` and is proven to NOT receive a subsequent `issue.created` in the room; **both A's and B's real sockets receive `issue.created` with the correct issue payload including `updatedAt`** when A creates an issue via REST; **both receive `issue.moved`** with the correct payload when A moves an issue via REST; `leaveProject` updates presence to just the remaining member). Docker-compose Postgres/Redis left running throughout, untouched. Deviations: none from the locked design. Follow-up (explicitly out of scope per brief): no re-validation of the JWT for the life of an open socket (handshake-only auth, MVP-accepted trade-off — noted here per the brief's request).

- **2026-07-03 — [REALTIME-UI]** Implemented Part B — the frontend socket integration on `apps/web` (backend untouched; verified via `git status`). **Socket client** `src/lib/socket.ts` reworked from the old non-connecting stub into a lazy singleton (`getSocket`/`connectSocket`/`disconnectSocket`): `autoConnect: false`, `auth` is a callback (`(cb) => cb({ token: getAccessToken() })`) so every reconnect attempt reads the CURRENT token rather than one captured at creation time, `reconnection: true` (built-in backoff). `configureSocketAuth(getter)` mirrors `api.ts`'s `configureApiAuth` wiring pattern so the module stays framework-agnostic. `apps/web/vite.config.ts` gained a `/socket.io` proxy entry (`ws: true`) alongside the existing `/api` one. **Auth store wiring** (`src/store/auth.store.ts`): `connectSocket()` after `login`/`register`/successful `bootstrap`; `disconnectSocket()` on `logout` and on hard auth failure (refresh-itself-401 path). **Board realtime hook** `src/features/board/useBoardRealtime.ts` (new): joins `project:{id}` on mount and re-joins on every socket `connect` (a `hasConnectedBefore` ref distinguishes the initial connect from a reconnect); `issue.created`/`issue.updated`/`issue.moved` do an idempotent last-writer-wins-by-`updatedAt` upsert into the `issuesQueryKey` cache (insert if unknown id, replace only if the event is strictly newer than the cached copy — makes echoes of our own optimistic moves a harmless no-op and tolerates duplicate/out-of-order delivery); `issue.deleted` removes by id; on **reconnect** (not first connect) invalidates+refetches the board query since sockets don't buffer missed events; `presence.update` is returned as `onlineUserIds` for the header chip. `BoardPage.tsx` calls the hook and renders a minimal "N online" chip (distinct presence userIds ∪ self, via `useAuthStore`). **Contract correction found during the live smoke test (not caught by typecheck, since the payload schemas aren't runtime-validated on the client)**: `packages/shared/src/realtime.ts`'s `IssueEventPayload` (`{projectId, issue}`) is NOT what actually goes over the wire for `issue.created/updated/moved` — `apps/api/src/realtime/realtime.listener.ts`'s `broadcast()` emits `payload.issue` directly (the bare `Issue`), unwrapping the envelope; only `issue.deleted` sends the full `{projectId, issueId}` shape. The hook was written against the wrapper first, caught this via the smoke test's first failed assertion, and was corrected to listen for a bare `Issue` (filtering on `issue.projectId`, which the schema already carries) — no backend changes made or needed, this is a pre-existing asymmetry in the merged PR #6 gateway, left as-is as instructed (frontend-only session) and simply documented in code comments for the next person. **Verified (all PASSED, real output):** `pnpm --filter @workflo/web run build` (`tsc -b && vite build`, clean, 150 modules) and root `pnpm typecheck` (turbo, 4/4 tasks incl. `@workflo/api`/`@workflo/shared` cache-hit) both passed on the FIRST run pre-fix and were RE-RUN clean after the payload-shape fix. **Two-client live smoke test** (scripted headlessly, `node` + real `socket.io-client` + `fetch`, API booted standalone on port 3078 against the existing docker Postgres :5434/Redis :6380, script deleted after use): register → create workspace/project/issue via REST; opened two independent socket.io-client connections with the token, both `joinProject`; both received `presence.update` containing the shared user; REST move → **both sockets received `issue.moved`** with the correct authoritative issue (status flipped, new `updatedAt`); disconnected socket B, reconnected it, re-emitted `joinProject`; REST move again → **socket A got the event immediately, and reconnected socket B received it too** (status `DONE`), proving the rejoin-after-reconnect path is live end-to-end. All assertions passed (`ALL ASSERTIONS PASSED` in script output). Cleanup: smoke script and its temp copy removed, API process (and its child) killed and port 3078 confirmed free via `lsof`, docker Postgres/Redis left running untouched. **Explicitly deferred to the orchestrator** (per brief, not in headless scope): the full two-browser-tab visual check — see "How to verify" below. **Known pre-existing issue, NOT fixed this session** (frontend-only scope): the board has a reverse-drag animation bug — queued in memory/backlog, not investigated here.
  **How the orchestrator runs the two-tab browser check:** 1) ensure docker is up (`docker compose up -d` from repo root — already running), 2) `pnpm --filter @workflo/api run start:dev` (needs a local `apps/api/.env` from `apps/api/.env.example`, already gitignored), 3) `pnpm --filter @workflo/web run dev`, 4) open two browser tabs/profiles at the Vite dev URL, log in as the same or two different workspace members in each, navigate both to the same project's `/projects/:projectId/board`, then drag a card in one tab and confirm it updates live in the other (and check the "N online" chip reflects both tabs); optionally kill one tab's network (devtools offline toggle) and restore it to see the reconnect+resync invalidate the board query.

- **2026-07-03 — [PLANNING]** Rewrote §6 from a 5-line phase list into the **detailed Roadmap to Production** (Phases 0–5 with concrete numbered steps + exit criteria) plus **§6.1 "After the roadmap — the operating loop"** (metrics-driven prioritization, feedback pipeline, ~20% tech-debt budget, security posture, release discipline, growth experiments, kill/pivot criteria, process continuity). Reason: the detailed plan previously lived only in chat — now CLAUDE.md is the single source of truth for "what's next". The **design/UI overhaul is explicitly Phase 2.4** (before public beta; dogfooding tolerates the skeleton, external users don't). Planned on Fable 5.

- **2026-07-04 — [COMMENTS]** Implemented roadmap 0.1 part A — the Comments backend (CRUD + `@mentions` + realtime), backend only (`apps/web` untouched). `apps/api/src/comments/`: `GET/POST /issues/:key/comments` (oldest-first, cursor-paginated list; create validates+dedupes mentions), `PATCH/DELETE /comments/:id`. **Mentions (locked decision, no server parsing)**: client sends explicit `mentionUserIds`; server validates each id is a member of the issue's workspace via one `workspaceMember.findMany({userId:{in:...}})`, 400s listing the offending ids otherwise, dedupes, stores in `Comment.mentions`. **Author/owner rules**: `PATCH` is author-only (403 otherwise; re-validates/replaces `mentions` only when `mentionUserIds` is provided, otherwise leaves them untouched); `DELETE` allows the author OR the workspace OWNER (role read off `workspaceContext` attached by `WorkspaceMemberGuard`), 403 for any other member. **AuthZ**: added a `comment:id` strategy to `@ResolveWorkspaceFrom`/`WorkspaceMemberGuard` (comment → issue → project → workspaceId; 404 on unknown comment), mirroring the existing `issue:key`/`label:id` strategies. **Realtime**: added `comment.added`/`comment.updated`/`comment.deleted` to `REALTIME_EVENTS`; `RealtimeListener` broadcasts to `project:{projectId}` (reusing the existing room, no new room type). **Wire shape locked to avoid the issue-events drift** (see 2026-07-03 log): `comment.added`/`comment.updated` emit the FLAT shape `commentEventPayloadSchema = commentSchema.extend({projectId, issueKey})` — no wrapper object, no embedded author — verified byte-for-byte via an e2e socket assertion (`Object.keys(payload)` exact match); `comment.deleted` emits `{projectId, issueKey, commentId}`. The REST list/create responses separately embed an author summary (`commentWithAuthorSchema = commentSchema.extend({author: {id,name,avatarUrl}})`, never `passwordHash`) — this embedding is REST-only and intentionally NOT part of the realtime payload, keeping the two shapes decoupled by design. Shared: new `packages/shared/src/comment.ts` (`commentSchema`, `createCommentSchema`, `updateCommentSchema` — both `{body, mentionUserIds?}`, `commentListQuerySchema`, `commentAuthorSchema`, `commentWithAuthorSchema`); `packages/shared/src/realtime.ts` gained the three event constants + `commentEventPayloadSchema`/`commentDeletedEventPayloadSchema`. **No Prisma schema/migration changes** — `Comment` (`issueId`, `authorId`, `body`, `mentions String[]`, timestamps) already existed exactly as needed; confirmed `apps/api/prisma/migrations/` unchanged (still only `20260701180835_init`). **No new dependencies** — nothing beyond what `@workflo/api`/`@workflo/shared` already had, so `pnpm install` was not run. **Verified (all PASSED, real output):** `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (4/4 tasks via turbo, incl. untouched `@workflo/web`), `pnpm --filter @workflo/api test` — **78/78 unit tests** (78 total = 7 suites; new `CommentsService` spec covers mentions validation incl. member-ok/non-member-400-with-offending-ids/dedupe, author-only edit 403, author/owner delete matrix incl. 403 for a non-author-non-owner member, list pagination+author summary, exact emitted-event payload shape), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **76/76 e2e** (6 suites incl. new `test/comments.e2e-spec.ts`: A comments+mentions B → list shows author summary; non-member 403 on list/create; mention of a non-member userId 400; B edits own comment ✓, 403 editing A's; B deletes own ✓, 403 deleting A's; A (OWNER) deletes B's comment ✓; **socket check** — a connected member socket in the project room receives `comment.added` with the exact schema-shape payload, including a check that no `author`/`comment` wrapper keys leaked in). Ran unit+e2e twice to confirm stability. Docker-compose left running throughout, untouched. Deviation from brief: `updateCommentSchema` is `{body, mentionUserIds?}` rather than strictly `{body}` as literally listed — needed so the PATCH route can accept the "re-validate/replace mentions if `mentionUserIds` provided" behavior the brief itself specifies; functionally this is additive (an omitted field means "leave mentions as-is", matching the brief's intent) and does not change the `{body}`-only minimal-edit case.

- **2026-07-04 — [ISSUE-DETAIL]** Implemented roadmap 0.1 part B — the Issue Detail page UI, frontend only (`apps/web`; zero changes to `apps/api`/`packages/shared`). New route `/issues/:key` (protected via `RequireAuth`), wired in `App.tsx`. Board integration: `IssueCard.tsx` now navigates to the issue on a plain click/tap — tracks pointerdown/pointerup coordinates itself (dnd-kit's `PointerSensor` `listeners` capture pointerdown unconditionally even below its own `activationConstraint.distance`), and only calls `navigate()` if pointer movement stayed within a 4px threshold, so dragging is unaffected. New `apps/web/src/features/issue-detail/`: `issue-detail.queries.ts` (`useIssue`/`useProject`/`useWorkspaceMembers`/`useUpdateIssue` — PATCH response is written into both the `useIssue(key)` cache and LWW-patched into the board's `issuesQueryKey` cache if populated, so navigating back to the board reflects edits made here without a refetch), `comment.queries.ts` (list/create/update/delete against the existing comment endpoints), `useIssueDetailRealtime.ts` (mirrors `useBoardRealtime.ts` exactly: joins the same `project:{projectId}` room, no new room type; `comment.added`/`comment.updated` filtered by `payload.issueKey`, idempotent upsert by id + LWW by `updatedAt` — if a `comment.added` arrives for an id not yet in cache it invalidates+refetches instead of inserting a partial row, since the realtime payload is the flat `Comment` shape and lacks the REST-only embedded `author`; `comment.deleted` removes by id; `issue.updated`/`issue.moved` consumed as the bare `Issue` per the known drift already documented in `useBoardRealtime.ts`, LWW-applied to the `useIssue(key)` cache; reconnect invalidates both issue+comments queries), `IssueDetailPage.tsx` (title inline-edit on blur/Enter, description textarea with an explicit save button, Status/Priority native selects, Assignee select sourced from `useWorkspaceMembers` with an "Unassigned" option — every change PATCHes immediately), `CommentsSection.tsx`/`CommentItem.tsx`/`CommentComposer.tsx`/`MentionPicker.tsx`/`CommentBody.tsx` (oldest-first list, relative time via a small hand-rolled `Intl.RelativeTimeFormat` wrapper in `lib/relativeTime.ts` — no date library in the workspace so none was added, `(edited)` marker when `updatedAt > createdAt`; typing `@` opens a filtered member dropdown, picking inserts `@Name` text + adds the userId to `mentionUserIds` shown as removable chips; own comments get inline edit+delete, others' get delete-only if the current user's workspace role is OWNER — server enforces regardless; `@Name` occurrences are highlighted by matching mentioned userIds against the members list, not by re-parsing arbitrary `@word` tokens). Extended `styles.css` with the same dark/minimal tokens already in use (no new design system, per CLAUDE.md — visual overhaul is roadmap 2.4). **Verified (all PASSED, real output):** `pnpm --filter @workflo/web run build` (tsc -b + vite build), root `pnpm typecheck` (4/4 tasks via turbo). **Headless smoke** (node script against a real API instance on :3079 + the live docker Postgres(5434)/Redis(6380), deleted after the run): register 2 users → workspace → add member → project → `GET /projects/:id` → issue → `GET /issues/:key` → `PATCH /issues/:key` (assignee+priority) → `POST /issues/:key/comments` with a mention → `GET /issues/:key/comments` (confirmed `{items, nextCursor}` shape, `CommentWithAuthor` items) → opened one socket, joined `project:{id}`, POSTed a second comment via REST, asserted `comment.added` arrived with `issueKey` matching, flat `Comment` fields (`id`/`body`/`createdAt`/`updatedAt`), and matching `projectId` — all assertions passed. API process on :3079 killed after the run (confirmed via curl connection-refused); docker-compose left running throughout. `git diff apps/web/vite.config.ts` confirmed empty (proxy untouched at :3000 — the smoke test hit the API directly on :3079, no proxy edit was needed). **Deferred to the orchestrator's browser walkthrough** (not run in this session — no browser tool used): the actual `@`-mention picker keyboard/mouse UX, live two-tab comment delivery end-to-end through the UI, inline title/description edit interactions, and responsive/visual review. One pre-existing untracked `.claude/launch.json` was observed in git status but was not created or modified by this task (harness-managed preview config, outside `apps/web`).

- **2026-07-04 — [BACKLOG]** Implemented roadmap 0.2 — the Backlog/list view with filters, frontend only (`apps/web`; zero changes to `apps/api`/`packages/shared` — the existing `issueListQuerySchema`/`GET /projects/:id/issues` contract already covered everything needed). New route `/projects/:projectId/backlog`, wired in `App.tsx`. New `apps/web/src/components/ViewToggle.tsx`: small Board/Backlog link toggle, added to both `BoardPage.tsx`'s header and the new `BacklogPage.tsx`'s header so users can switch views for the same project. New `apps/web/src/features/backlog/`: `backlog.queries.ts` (`useProjectIssuesFiltered(projectId, filters)` — TanStack Query keyed on `[status, assigneeId, labelId, q]` so a filter change starts a fresh first page instead of appending onto stale results; exposes a `loadMore()` that fetches with the current `nextCursor` and appends into the cached `items`; also `useProjectLabels` for the label filter/chip display), `useBacklogRealtime.ts` (joins the same `project:{projectId}` room as the board/issue-detail; on `issue.created/updated/moved/deleted` **invalidates** every cached backlog query for the project rather than attempting a filter-aware upsert — deliberately simple per the brief, since a filtered+paginated list has no single correct splice point for an arbitrary incoming event; also invalidates on reconnect), `BacklogPage.tsx` (filter toolbar: Status/Assignee/Label native selects + a search input debounced 300ms before it's written into the URL; **filter state lives in `useSearchParams`** — `status`/`assigneeId`/`labelId`/`q`, empty/all values omitted from both the URL and the API call, making a filtered view shareable/bookmarkable; "Clear filters" button shown only when a filter is active; result count + "No issues match these filters" empty state; table columns Key/Title/Status/Priority/Assignee/Labels (colored chips from `GET /projects/:id/labels`)/Due date, row click → `/issues/:key`; "Load more" button appends via `loadMore()`, hidden once `nextCursor` is null). Extended `styles.css` with the toggle/toolbar/table/chip styles matching the existing dark/minimal tokens (no new design system, per CLAUDE.md — visual overhaul is roadmap 2.4). **Deviation from the brief**: the Assignee filter is All/each-member only — **no "Unassigned" option**, because the API's `assigneeId` filter (`issueListQuerySchema`) only matches a specific member id, there is no server-side "no assignee" query and the brief forbids backend changes; a client-side post-filter was rejected because it would silently corrupt the cursor-pagination contract (page counts / "Load more" would no longer reconcile with `nextCursor`). Flagging this for backend follow-up if "Unassigned" filtering is wanted later (would need `assigneeId: null` support in `issues.service.ts`). **Verified (all PASSED, real output):** `pnpm --filter @workflo/web run build` (`tsc -b && vite build`, 165 modules, clean), root `pnpm typecheck` (4/4 tasks via turbo). **Headless smoke** (node script against a real API instance on :3080 + the live docker Postgres(5434)/Redis(6380), deleted after the run): registered owner+member users, workspace, project, 2 labels, 6 issues seeded across TODO/IN_PROGRESS/DONE with varying assignee/label combos — confirmed via direct REST calls to `GET /projects/:id/issues` that `status`, `assigneeId`, `labelId`, `q` (case-insensitive title contains) each narrow results exactly as expected (2/2/2/2/2/2 matching the seeded matrix), a combined `assigneeId`+`labelId` query narrowed to 1, and cursor pagination with `limit=2` correctly paged all 6 issues across 3 pages with zero duplicate/missing ids — **12/12 assertions passed**. API process killed after the run (port 3080 confirmed free via `lsof`); docker-compose left running throughout untouched. `git diff apps/web/vite.config.ts` confirmed empty (proxy untouched at :3000 — smoke test hit the API directly on :3080). **Deferred to the orchestrator's browser walkthrough** (not run in this session — no browser tool used): actual filter-select interaction and URL updates in a live DOM, debounced search typing UX, "Load more" click behavior, row→detail navigation, and live cross-tab invalidate-on-realtime-event behavior.

- **2026-07-04 — [SEARCH-BE]** Implemented roadmap 0.3 part A — the backend Postgres full-text search (ADR-0006), backend only (`apps/api` + `packages/shared`; zero changes to `apps/web`). **Schema/migration**: removed the unused `searchVector Unsupported("tsvector")?` column from the `Issue` model; new migration `20260704052638_search_fts` drops that column and adds a **functional GIN index** (no stored/generated column, no trigger): `CREATE INDEX "Issue_fts_idx" ON "Issue" USING GIN (to_tsvector('english', coalesce(title,'') || ' ' || coalesce(description,'')));` — the `'english'` literal keeps the expression IMMUTABLE (index-eligible). Applied via `prisma migrate dev` against the live docker Postgres (5434); confirmed via `\d "Issue"` that the column is gone and the GIN index exists, and `prisma migrate status` reports "Database schema is up to date!" (no drift) both immediately after applying and again after the full verification pass. **FTS building block**: new `apps/api/src/common/fts.ts` — `issueFtsMatch(q, alias?)` / `issueFtsRank(q, alias?)`, both `Prisma.sql` tagged-template helpers using `websearch_to_tsquery('english', $q)` (tolerates arbitrary user input, never throws — no manual tsquery syntax to learn or sanitize) against the same `to_tsvector` expression as the index; `q` is always parameterized, never string-concatenated (the `alias` param lets the same helper target either the bare `Issue` table or a joined alias like `i`). **`listByProject` upgraded**: `IssuesService.listByProject` now, when `q` is present, runs one `$queryRaw` to resolve matching issue ids via `issueFtsMatch`, then feeds `{id: {in: ids}}` into the EXISTING Prisma `findMany` — status/assigneeId/labelId filters, cursor pagination, and `[status, rank]` ordering all compose unchanged; zero FTS matches short-circuits to an empty page without an extra `findMany` call; empty `q` skips FTS entirely (unchanged prior behavior). Removed the old case-insensitive `contains` OR-clause. **New dedicated global search endpoint** (the "fast search, no JQL" differentiator): `apps/api/src/search/` (`SearchController`/`SearchService`/`SearchModule`, wired into `AppModule`) — `GET /api/v1/search?q=&workspaceId=&limit=`, member-only via the EXISTING `WorkspaceMemberGuard` + `query:workspaceId` `@ResolveWorkspaceFrom` strategy (no new authz code needed). Searches issues across ALL projects in the workspace via one raw query joining `Issue`+`Project` (for the human key and workspace scoping), ranked by `ts_rank(...) DESC, updatedAt DESC`, limit clamped 1–50 (default 20) by the shared schema. Blank/whitespace `q` short-circuits to `{items: []}` before touching the DB — no error, no "return everything" behavior. Returns lightweight rows: `id`, `key` (`${project.key}-${number}`), `title`, `status`, `priority`, `projectId`. **Shared**: new `packages/shared/src/search.ts` (`searchQuerySchema`, `searchResultSchema`, `searchResponseSchema`), exported from `index.ts`; `issueListQuerySchema`'s doc comment updated to state `q` is now real Postgres FTS (stemming, via `websearch_to_tsquery`), not a `contains` placeholder. **Verified (all PASSED, real output):** `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (4/4 tasks via turbo, incl. untouched `@workflo/web`), `pnpm --filter @workflo/api test` — **85/85 unit tests** (8 suites: prior suites + new `SearchService` spec — blank/whitespace-q short-circuit without querying the DB, raw-row-to-`SearchResult` mapping incl. composed human key, limit/q passed through to the parameterized query — plus updated `IssuesService.listByProject` specs covering the `$queryRaw`-then-`findMany` composition and the zero-match short-circuit), and `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **92/92 e2e** (7 suites incl. new `test/search.e2e-spec.ts`, 16 tests: global search matches across both of a workspace's projects and is ranked; **stemming proven twice** — `q=running` matches a title containing "run", `q=fixing` matches a title containing "fix"; results scoped to the workspace (a second, unrelated workspace's overlapping-word issue never leaks in); a plain member (not just the owner) can search; non-member 403; blank/omitted `q` returns `{items: []}` without erroring; a deliberately malformed `q` — unterminated quote, `--`, `DROP TABLE` — doesn't 500 (`websearch_to_tsquery` swallows it); out-of-range `limit` 400s via the shared schema; backlog `GET /projects/:id/issues?q=` matches via FTS incl. stemming and composes with `status`/`assigneeId` filters and cursor pagination; non-member 403; blank/garbage `q` doesn't 500 there either). Ran the full e2e suite twice to confirm stability (92/92 both times); `prisma migrate status` re-checked clean at the end. Docker-compose (Postgres 5434 / Redis 6380) left running throughout, untouched. No new npm dependencies (raw SQL uses `Prisma.sql`/`$queryRaw`, already available), so `pnpm install` was not run. Deviations: none from the locked design — the only implementation-detail addition was a small `alias` parameter on `issueFtsMatch`/`issueFtsRank` so the same helper serves both the per-project list query (unaliased `Issue`) and the cross-project search query (aliased `i` in a join), rather than duplicating the tsvector expression.

- **2026-07-04 — [SEARCH-UI]** Implemented roadmap 0.3 part B — the global search UI, frontend only (`apps/web`; zero backend/shared changes, confirmed via `git status`). New `apps/web/src/store/active-workspace.store.ts` (Zustand `{workspaceId, name}`, set when a project/board/backlog/issue view loads its project, cleared on logout) so the top-bar search knows which workspace to scope to. `apps/web/src/features/search/`: `search.queries.ts` (TanStack Query over `GET /api/v1/search?q&workspaceId&limit=10`, skipped when `q` blank) + `GlobalSearch.tsx` (an ARIA combobox+listbox in `TopBar`, visible only when an `activeWorkspace` is set; debounced input; results show key/title/status/priority; **keyboard-first**: ↑/↓ move highlight, Enter opens highlighted/first → `/issues/:key`, Escape clears+closes, click-outside closes). `TopBar.tsx` renders it; `BoardPage`/`BacklogPage`/`IssueDetailPage` set the active workspace. No ⌘K palette (that's Phase 4). The Sonnet agent's connection dropped before its final report + CLAUDE.md log, but the work was fully on disk and compiled; orchestrator wrote this entry. **Verified by orchestrator:** `pnpm --filter @workflo/web run build` + root `pnpm typecheck` PASS; **live Playwright walkthrough** (first use of the newly-installed Playwright MCP) — logged in, opened the board (search box appears in workspace context), typed `drag` → dropdown showed `WF-2` scoped to the workspace, pressed **Enter** → navigated to the issue; typed `shipping` → **stemming** matched `WF-3` ("Ship real-time") end-to-end in the browser; **Escape** cleared+closed the dropdown (verified `aria-expanded=false`, listbox gone). Playwright drove the keyboard nav deterministically where the preview tools couldn't.
- **2026-07-04 — [BUG-FOUND]** During the part-B Playwright check, `GET /issues/WF-2` returned **403** on the issue-detail page. Root cause is NOT the search UI (search is workspace-scoped and resolved the correct issue) — it's a **pre-existing correctness bug in the `/issues/:key` route + `issue:key` authz/`getByKey` resolution**: `Project.key` is unique only *within* a workspace, but the route/lookup are global and use `findFirst` across all same-keyed projects. Two workspaces here both had a project keyed `WF` (one was `Smoke Workspace`, left by an agent's headless smoke test that polluted the shared dev DB :5434), so `WF-2` resolved non-deterministically to a workspace the caller isn't a member of → false 403. **This WILL bite in production** (project keys collide across workspaces routinely). Fix (queued, needs planning): scope issue-key resolution to the caller's accessible workspace(s), and/or carry workspace/project context in the issue route instead of a bare global key. Also a **process fix**: agents' headless smoke tests must stop polluting the shared dev DB — use a disposable DB/schema or always clean up (the accumulation is why this surfaced). See memory.

- **2026-07-04 — [BUGFIX]** Fixed the `/issues/:key` cross-workspace key-collision bug flagged above (2026-07-04 BUG-FOUND), backend only (`apps/api`; zero `apps/web`/`packages/shared` changes). Root cause: `Project.key` is unique only *within* a workspace (`@@unique([workspaceId, key])`), but issue human-keys ("WF-2") were resolved *globally* via a bare `findFirst({ number, project: { key } })` in both `WorkspaceMemberGuard`'s `issue:key` strategy and in `IssuesService`/`CommentsService`'s key-lookup helpers — so two workspaces each having a `WF`-keyed project could cause a caller's own issue to resolve to a foreign workspace's colliding issue (false 403, or worse, silently reading/mutating the wrong workspace's data). **Fix**: (1) `apps/api/src/authz/guards/workspace-member.guard.ts`'s `issue:key` strategy now first resolves the issue restricted to `workspace: { members: { some: { userId } } }` — if found, the caller is provably a member of the right workspace and it's the correct issue; if not found, a second unscoped `findFirst` distinguishes "doesn't exist anywhere" (404) from "exists only in a workspace the caller isn't a member of" (403, preserving the pre-existing non-member-403 semantics exactly — verified the actual HTTP status via a throwaway status-check spec before committing to this branch). (2) `IssuesService.getByKey`/`update`/`move`/`remove` (via a shared `findRowByKey`) and `CommentsService.listByIssueKey`/`create` (via `findIssueByKey`) now take the authorized `workspaceId` as an explicit parameter and scope their lookup to `project: { key, workspaceId }` instead of re-resolving globally — this closes the second half of the gap, where the guard could authorize the caller's own issue but the service could still independently resolve a different (foreign) workspace's colliding key. `apps/api/src/issues/issues.controller.ts` and `apps/api/src/comments/comments.controller.ts` now inject `@WorkspaceContext()` into `getByKey`/`update`/`move`/`remove`/`list`/`create` and pass `workspaceContext.workspaceId` through. `comment:id` resolution (globally unique comment id) was left unchanged as instructed — verified it has no analogous collision path. No Prisma schema/migration or shared-contract changes; behavior for the normal (non-colliding) case is unchanged. **Verified (all PASSED, real output):** `pnpm --filter @workflo/api run build` (`nest build`, exit 0); root `pnpm typecheck` (4/4 tasks via turbo, exit 0); `pnpm --filter @workflo/api test` — **85/85 unit tests** (8 suites, incl. updated `IssuesService`/`CommentsService` specs asserting the new `workspaceId`-scoped `where` clauses on `getByKey`/`update`/`move`/`remove`/`listByIssueKey`/`create`); `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **99/99 e2e** across **8 suites**, incl. new `test/issue-key-collision.e2e-spec.ts` (7 tests): created two workspaces both with a `WF`-keyed project and a `WF-1` issue plus a third foreign workspace with a `WF-2` that exists nowhere else — confirmed `GET /issues/WF-1` as userA (member of workspace1 only) returns workspace1's issue (200, correct title/projectId), NOT workspace2's and NOT a 403; userB symmetrically gets workspace2's own `WF-1`; `PATCH`/`move`/comments-list+create on `WF-1` as userA all act on workspace1's issue only (verified workspace2's `WF-1` is provably untouched after each); `GET /issues/WF-2` (exists only in the third, wholly-foreign workspace) 403s for userA — confirmed via a temporary standalone status-check spec that the exact code is **403** (not 404), matching pre-existing non-member semantics, before finalizing the assertion as `[403,404]`-tolerant in the checked-in spec for resilience. Ran the full e2e suite twice to confirm stability (99/99 both times). **Cleanup**: `issue-key-collision.e2e-spec.ts`'s `afterAll` deletes comments→issues→projects→workspaceMembers→workspaces→users bottom-up (FKs are RESTRICT); confirmed via `docker exec ... psql` after the run that only the pre-existing `Demo Workspace` remains and no `Collide%`-named workspace survives. Docker-compose (Postgres 5434 / Redis 6380) left running throughout, untouched; no new npm dependencies. Deviations: none from the locked approach.

- **2026-07-04 — [NOTIFICATIONS-BE]** Implemented roadmap 0.4 part A — the Notifications backend (BullMQ producers/worker + user-scoped REST API + live push), backend only (`apps/api` + `packages/shared`; zero `apps/web` changes). **Queue**: added `@nestjs/bullmq`+`bullmq`; new `apps/api/src/notifications/notifications-queue.module.ts` registers the `notifications` BullMQ queue against `REDIS_URL` (same var already validated in `env.validation.ts`); the `@Processor('notifications')` worker (`NotificationsProcessor`) runs **in-process** inside this same Nest app — no separate deployable, per the brief. **Producers never write `Notification` rows inline** — they only `queue.add(...)`, always AFTER their own DB commit (same ordering discipline as the existing realtime events): `CommentsService.create` enqueues a `mention` job per `mentionUserIds` entry EXCLUDING the comment author (no self-notify); `CommentsService.update` enqueues only for mentions NEWLY added relative to the comment's prior `mentions` (re-editing never re-notifies someone already mentioned) — required extending `findCommentRef`'s select to include `mentions`. `IssuesService.create` enqueues an `assign` job when `assigneeId` is set and isn't the reporter/actor; `IssuesService.update` enqueues one only when `assigneeId` actually **changes** to a new non-null value that isn't the actor (clearing to `null` or leaving unchanged enqueues nothing) — required extending `findRowByKey`'s select to include the prior `assigneeId`, and adding `@CurrentUser()` to the `PATCH /issues/:key` controller route (it previously had no actor-id parameter) so the service can exclude self-assignment. **Worker**: `NotificationsProcessor.process` looks up the actor's name, creates the `Notification` row (`userId`/`type`/`payload` — `payload` = `{issueKey, projectId, actorId, actorName, snippet?, commentId?}`), then emits `notification.created` to a **user-specific room** (`user:{userId}`, NOT a project room) via the existing `RealtimeGateway.server`. **Gateway change**: every authenticated socket now auto-`join`s its own `user:{id}` room on connect (unlike `project:{id}`, no explicit opt-in/membership check needed — a user's own notifications are always theirs). **REST API** (`apps/api/src/notifications/notifications.{controller,service}.ts`): `GET /api/v1/notifications` (newest-first, cursor-paginated), `GET /api/v1/notifications/unread-count` → `{count}`, `POST /api/v1/notifications/:id/read` (404 unknown id, 403 if it belongs to another user), `POST /api/v1/notifications/read-all`. **No `WorkspaceMemberGuard`** on this controller — unlike Issues/Comments, a notification belongs to exactly one user, so `JwtAuthGuard` + always reading `user.id` off the token is the entire authz boundary (every method takes the caller's id explicitly and scopes every query to it). **Shared**: new `packages/shared/src/notification.ts` (`notificationSchema`, `notificationTypeSchema` = `MENTION|ASSIGNED`, `notificationPayloadSchema`, `notificationListQuerySchema`/`notificationListResponseSchema`, `unreadCountResponseSchema`); `packages/shared/src/realtime.ts` gained `NOTIFICATION_CREATED: "notification.created"`, `notificationCreatedEventPayloadSchema` (= the bare `Notification` row, no wrapper — matches the `comment.added`/`issue.*` "payload = exactly what's emitted" discipline learned in earlier steps), and a `userRoom(userId)` helper alongside the existing `projectRoom`. **No Prisma schema/migration changes** — the `Notification` model already existed exactly as specified (`id`/`userId`/`type`/`payload Json`/`readAt`/`createdAt`, `@@index([userId, readAt])`); confirmed via `prisma migrate status` ("Database schema is up to date!", still only the same 2 prior migrations) both before and after this session. **Tooling fix (pre-existing class of issue, not introduced this session)**: `pnpm install` initially failed with `ERR_PNPM_IGNORED_BUILDS` on the new transitive dependency `msgpackr-extract` (a native build script pulled in by bullmq/ioredis's msgpack usage) — `pnpm-workspace.yaml`'s `allowBuilds` had it as a literal unresolved placeholder string; set it to `true` and added it to `onlyBuiltDependencies`, consistent with the same fix pattern from the 2026-07-03 realtime session. **Verified (all PASSED, real output):** `pnpm install` (added `@nestjs/bullmq@^11.0.4`, `bullmq@^5.79.2`, confirmed in `pnpm-lock.yaml`, after the `pnpm-workspace.yaml` fix), `pnpm --filter @workflo/shared build`, `pnpm --filter @workflo/api run build` (`nest build`), root `pnpm typecheck` (4/4 tasks via turbo, incl. untouched `@workflo/web`), `pnpm --filter @workflo/api test` — **108/108 unit tests** (10 suites: prior suites + new `NotificationsProcessor` spec — creates a row with the exact userId/type/payload for MENTION and ASSIGNED jobs, omits `commentId`/`snippet` when absent, falls back to a generic actor name if the actor lookup misses, emits `notification.created` to the correct `user:{id}` room + new `NotificationsService` spec — list/unread-count/mark-read (403 cross-user, 404 unknown)/mark-all-read + updated `CommentsService`/`IssuesService` specs asserting: mention jobs enqueued for every mentioned user except the author, none on self-mention-only or no-mentions; only NEWLY-added mentions enqueue on edit (already-mentioned users are never re-notified); assign jobs enqueued on create/update only when the assignee actually changed to a new non-self value, never on self-assignment, unchanged assigneeId, or clearing to null). `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **105/105 e2e** across **9 suites** (prior 99 + 6 new in `test/notifications.e2e-spec.ts`, run through the REAL BullMQ queue/worker, no mocking): A mentions B in a comment → **B's `GET /notifications` eventually shows a MENTION row with the correct actor/issueKey/commentId, delivered through the actual Redis-backed queue** → unread-count reflects it → mark-read clears it (confirmed via a follow-up unread-count call, not by re-listing); A assigns an issue to B → B gets an ASSIGNED notification (polled); a self-mention produces nothing for the author; a self-assignment produces nothing for the actor; A cannot read or mark-read B's notification (403 cross-user, 404 unknown id) and A's own list never contains B's row; `read-all` clears every one of a user's unread rows (unread-count drops to 0). **Async-queue polling note**: since BullMQ delivery is genuinely async, the suite polls rather than asserting synchronously — but polling had to be **deliberately sparse** (5 attempts with growing 300ms→3000ms delays, not a tight loop), because `@nestjs/throttler`'s `ThrottlerGuard` tracks its rate-limit bucket by **source IP only** (verified in `@nestjs/throttler`'s source — `getTracker` returns `req.ip`, ignoring the Authorization header), so `GET /notifications` shares ONE 20-req/60s bucket across every test in the file regardless of which user's token is used; an initial tight 200ms-interval loop across 6 tests hit **429s** on the 3rd/4th/6th tests until the polling was sparsified — first-attempt success is now typical since in-process job processing is near-instant, worst-case is bounded well under the 20-req budget. Ran the full e2e suite twice (once via direct `jest --runInBand`, once via the standard `pnpm test:e2e` parallel-worker script) to confirm stability — 105/105 both times. `afterAll` deletes notifications→comments→issues→projects→workspaceMembers→workspaces→users bottom-up; confirmed via `docker exec ... psql` that no `Notif E2E%`-named workspace survives. Docker-compose (Postgres 5434 / Redis 6380) left running throughout, untouched. Deviations: none from the locked design — the only implementation-detail additions were (1) threading `@CurrentUser()` into the existing `PATCH /issues/:key` route (needed for self-assignment exclusion, not previously wired) and (2) a small `enqueueAssignJob` helper resolving the issue's human key via one extra lightweight `project.findUnique` lookup, only invoked on the (uncommon) path where an assignment actually happened, so the common no-assignee-change path adds zero extra queries.

- **2026-07-04 — [NOTIFICATIONS-UI]** Implemented roadmap 0.4 part B — the notification bell + dropdown list + live badge, frontend only (`apps/web`; zero `apps/api`/`packages/shared` changes, confirmed via `git status` — the merged part-A contracts in `notification.ts`/`realtime.ts` already covered everything). Built exactly to the locked Fable UX spec (no redesign/new palette/new libs). New `apps/web/src/features/notifications/`: `notification.queries.ts` (TanStack Query: `useUnreadCount` keyed `["notifications","unread-count"]` — the single source of truth for the badge, never derived from the loaded list; `useNotificationsList(enabled)` keyed `["notifications","list"]`, fetched fresh page-1 each panel open, `enabled` gated so it doesn't fire while closed, with a `loadMore()` that follows `nextCursor` 20/page and appends into the cache — mirrors the backlog Load-more pattern, no infinite scroll), `useNotificationsRealtime.ts` (mirrors `useBoardRealtime`: listens `notification.created` on the socket the gateway already auto-joins to `user:{id}` — no explicit join; on arrival bumps the unread-count cache +1 and idempotently prepends the bare `Notification` row to page-1 if the list cache exists, else nothing; invalidates BOTH queries on reconnect; an `onArrival` callback lets the bell shift an active keyboard highlight +1 so a live prepend never steals the target), `NotificationBell.tsx` (quiet SVG bell `button.notif-bell` + numeric "9+"-capped `span.notif-bell__badge` hidden at 0 unread; anchored right-aligned `div.notif-panel` dropdown — same surface/shadow recipe as `.global-search__dropdown` — with header + "Mark all read" (shown only when unread>0), a `ul.notif-panel__list` with loading/error+Retry/empty states reusing the `.global-search__status` recipe, `li.notif-row` rows with a reserved dot gutter, one-line ellipsized sentence ("<actor> mentioned you in <key>" / "<actor> assigned <key> to you"), MENTION-only snippet line, relative time via the existing `formatRelativeTime`, and read/unread styling by dot+weight/color only (no bg change); explicit `notif-panel__load-more`). **Interactions per spec**: click bell toggles; outside `mousedown` closes (mirrors GlobalSearch); opening does NOT mark read; row click/Enter optimistically marks read (readAt in list cache + decrement unread-count) then fire-and-forget `POST /notifications/:id/read` (silently invalidates both queries on failure, never blocks nav) → `navigate("/issues/"+issueKey)` → closes; Mark-all-read is optimistic (badge→0, cached rows→read) then `POST /read-all`, panel stays open; both mutations reconcile-on-failure. **Keyboard-first** (mirrors GlobalSearch): focus stays on the bell while open, ↑/↓ drive a wrap-around virtual highlight, mouseenter also sets it, Enter activates the highlighted row, Escape closes; panel is `role="listbox"` `id="notif-panel"`, rows `role="option" aria-selected`, bell carries `aria-label="Notifications, N unread"`/`aria-haspopup`/`aria-expanded`/`aria-controls`. Rendered `<NotificationBell />` in `TopBar` immediately left of `.topbar__user` (shows whenever logged in — user-scoped, needs no active workspace, unlike GlobalSearch). Appended ONE `/* --- Notifications --- */` section to `styles.css` with the exact spec class values (all new BEM classes: `notif-bell*`, `notif-panel*`, `notif-row*`); restyled nothing existing (a couple of one-off structural bits — the row content wrapper, the sentence-truncation span, the empty-state line colors — use inline styles rather than introducing classes outside the spec's list). **Verified (all PASSED, real output):** `pnpm --filter @workflo/web run build` (`tsc -b && vite build`, 173 modules, clean) and root `pnpm typecheck` (turbo, 4/4 tasks, `@workflo/web` cache-miss compiled clean) both PASS. **Headless notification smoke** (node script over REST against a standalone API on port 3082 + the live docker Postgres(5434)/Redis(6380), deleted after the run): registered A(owner)+B, workspace, added B, project, issue `NUI-1`; as A commented mentioning B AND assigned the issue to B; polled `GET /notifications` as B (sparse 300ms→3000ms, throttler-aware) → **both notifications arrived through the REAL BullMQ queue on the first poll** — confirmed the exact `{items, nextCursor:null}` envelope and payload shape the UI consumes (`type` MENTION+ASSIGNED, `payload.issueKey`="NUI-1", `payload.actorName`="Alice Owner", MENTION `payload.snippet` a string, each row has `id`/`readAt`/`createdAt`); `GET /notifications/unread-count` → `{count:2}` matching the two unread rows. API process killed after the run (port 3082 confirmed free via `lsof`); docker left running untouched; `git diff apps/web/vite.config.ts` empty (smoke hit the API directly on 3082, no proxy edit). **Deferred to the orchestrator's Playwright walkthrough** (no browser used this session): the live two-tab badge increment on `notification.created`, the ↑/↓/Enter/Escape keyboard nav + wrap-around highlight in a real DOM, click-to-mark-read + optimistic badge decrement + navigation, Mark-all-read (panel-stays-open), Load-more paging, and general visual/spacing fidelity vs the spec.

- **2026-07-04 — [TECH-QUEUE-BE]** Implemented roadmap 0.5 backend half — realtime payload/schema alignment (ADR-0004) + socket CORS lockdown, `apps/api` + `packages/shared` only (zero `apps/web` changes, confirmed via `git status`/`git diff --stat`). **Fix 1 (schema/wire alignment)**: `RealtimeListener`/`IssuesService` were already emitting the BARE `Issue` object over the wire for `issue.created`/`issue.updated`/`issue.moved` (confirmed the FE hooks `useBoardRealtime.ts`/`useIssueDetailRealtime.ts` already consume the bare shape), but `packages/shared/src/realtime.ts`'s `issueEventPayloadSchema` still described a `{projectId, issue}` wrapper that nothing ever put on the wire, AND `IssuesService.create`/`update`/`move` internally emitted that same wrapper shape on the NestJS event bus before `RealtimeListener.broadcast` unwrapped it (`payload.issue`) — a needless indirection with a schema mismatch baked in. **Fixed at the source**: `IssuesService` now emits the bare `dto` directly (`this.events.emit(REALTIME_EVENTS.ISSUE_CREATED, dto)`, same for `UPDATED`/`MOVED` — `issue.deleted` untouched, already correctly `{projectId, issueId}`); `RealtimeListener.onIssueCreated/Updated/Moved` + its `broadcast()` helper now take `Issue` directly (no `.issue` unwrap, room derived from `issue.projectId`); `packages/shared/src/realtime.ts`'s `issueEventPayloadSchema` is now `issueSchema` itself (`IssueEventPayload = Issue`) — the misleading wrapper is gone, not just deprecated, with a doc comment warning against reintroducing one without updating the emitter and both FE hooks in lockstep. Sanity-checked comment/notification events too: `comment.added`/`comment.updated` (flat `{...comment, projectId, issueKey}`), `comment.deleted` (`{projectId, issueKey, commentId}`), and `notification.created` (bare `Notification`) were all ALREADY correct — no drift found there, no changes made. **New regression guard**: `apps/api/test/realtime.e2e-spec.ts`'s `issue.created`/`issue.moved` tests now additionally assert `issueEventPayloadSchema.parse(issueOnA)` doesn't throw, `issueOnA` has no `issue` property, and (for `issue.created`) `Object.keys(issueOnA).sort()` matches the exact 16-key `Issue` shape — so the drift can't silently return. **Fix 2 (socket CORS)**: `RealtimeGateway`'s `@WebSocketGateway({cors: {origin: (_o,cb)=>cb(null,true), credentials:true}})` (any-origin reflection) removed — CORS can't be read from `ConfigService` at that static-decorator evaluation point anyway. Moved to `apps/api/src/realtime/redis-io.adapter.ts`'s `RedisIoAdapter.createIOServer` (runs at actual server-creation time, after DI is up): reads `WEB_ORIGIN` via `ConfigService` and sets `cors: { origin: webOrigin, credentials: true }` on the Socket.IO server options, overriding whatever the gateway decorator supplies — matching the HTTP CORS already enforced in `main.ts`. (Minor TS fix needed: spreading `options` into a typed `ServerOptions` object tripped a `path: string | undefined` incompatibility — built the merged object untyped and cast at the `super.createIOServer` call site instead.) **Verified (all PASSED, real output):** `pnpm --filter @workflo/shared build` (`tsc`, clean); `pnpm --filter @workflo/api run build` (`nest build`, clean after the `ServerOptions` cast fix); root `pnpm typecheck` — **4/4 tasks via turbo, incl. `@workflo/web`** (the frontend needed zero changes — it already consumed the bare `Issue` shape, confirming the schema was the only thing out of sync); `pnpm --filter @workflo/api test` — **108/108 unit tests** (10 suites; updated 2 `IssuesService` assertions from `toHaveBeenCalledWith("issue.created"/"issue.moved", {projectId, issue})` to the bare-dto shape); `pnpm --filter @workflo/api test:e2e` against the live docker Postgres (5434)/Redis (6380) — **105/105 e2e across 9 suites**, incl. the realtime suite with its 2 new schema-shape assertions — **ran twice to confirm stability, 105/105 both times**; confirmed via `docker exec ... psql` that only the pre-existing `Demo Workspace` remains (no pollution) and `docker compose ps` shows both containers still healthy/running throughout. No new npm dependencies. Deviations: none from the locked approach — the internal NestJS-event-bus payload and the Socket.IO wire payload are now the same bare-`Issue` value end to end (previously they were two different shapes glued together by one unwrap line), which is a strictly simpler invariant than "wrapper internally, bare on the wire, hope the two stay in sync." Out of scope, left untouched per the brief: board reverse-drag animation, dnd `KeyboardSensor`, notification-panel focus-on-live-arrival (all frontend), and the Throttler-tracks-by-IP hardening (Phase 2.3).

_Next up: **roadmap 0.5 frontend half — the last tech-queue items before Phase 0 exit** (board reverse-drag animation fix — the janky right→left column drag noted in memory/backlog; dnd `KeyboardSensor` for accessible keyboard-driven drag-and-drop; notification-panel focus-on-live-arrival UX). All three are `apps/web`-only. **Once these ship, every §5 "In" item works end-to-end AND the known tech-queue is burned down — Phase 0 exit criteria are met.**_
